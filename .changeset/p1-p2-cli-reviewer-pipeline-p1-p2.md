---
"@oscaner-skills/osuperpowers": minor
"@oscaner-skills/osuperpowers-router": patch
---

cli-reviewer-pipeline (P1+P2): unified reviewer CLI system — four review types (spec-review/plan-review/task-review/branch-review) with structured naming, `cdd-review` (all reviews) and `cdd-task` (per-task chain) replace `cdd-exec`/`cdd-run`, `review` mode renamed to `task-review`