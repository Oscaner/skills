---
"@oscaner-skills/cdd-engine": patch
---

Residue save-family single-owner convergence (P6 spec T7.7, task T28): the SAVE side of the residue lifecycle now lives entirely in `artifacts/residue.ts` — eligibility (`recoveryEligible` / `RESIDUE_PRESERVED_CAUSES`, derived from `FAILURE_CATEGORIES`, AC14 no hand-written literals), the carrier-anchored adapter `settleFromCarrier` (base `settleResidue` template step + branch inline lane), the `preserved` idempotence guard, and the `CDD_WARN` announce wrapper (`preserveAndAnnounceResidue`). Every save path emits ONE standardized stash message (`cdd-<op>-<type>-task-<N>-r<round>-<cause>` — the contract the task lane already produced); the pre-T28 double-owner defect (an implement TIMEOUT round wrote a second, bespoke-message stash for the same WIP) is gone — the lane pre-writes `recovery.preserved=true` and a coexisting hook's adapter run is an idempotent no-op. `rules/residue.ts` and `infra/git.ts#gitStashPreserve` are deleted; `recovery.wip_stat` (structured WIP scale incl. brand-new untracked files) archives beside `residue_scope`. `roundFromCarrierBasename` returns the numeric round for every family (implement → 1, round-bearing names carry their own, unclassifiable → null), so the standardized message always embeds `r<round>` without a duplicate marker.