# Release flow (main only):
#   develop accumulates .changeset/*.md → develop→main PR → push main
#   → changesets/action opens Version PR (target main); hasChangesets=true → tag/Release/sync skipped
#   → merge Version PR → push main again; hasChangesets=false → publish mode
#   → per-plugin matrix job pushes git tag + GitHub Release only for plugins that
#     were actually versioned (recorded in .changeset/versioned-plugins.json by
#     version-packages.mjs); plugins with no changesets are skipped — no phantom
#     baseline tag/release → sync PR.
#   Vendors (superpowers/mattpocock-skills/impeccable) co-publish in publish mode:
#     publish-vendor (npm assembly publish + registry gap sweep → to_tag JSON)
#     → release-vendor (git tag + GitHub Release per gap item; upstream-sync body).
#     release-plugin and sync-develop block on publish-vendor (atomicity).
name: Release

on:
  push:
    branches: [main]

concurrency: release-${{ github.ref }}

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    runs-on: ubuntu-latest
    outputs:
      hasChangesets: ${{ steps.changesets.outputs.hasChangesets }}
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm run emit
      - uses: changesets/action@v2
        id: changesets
        with:
          version: node scripts/version-packages.mjs
          publish: pnpm exec changeset publish
          createGithubReleases: false
          commit: "chore: release plugins"
          title: "chore: release plugins"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-vendor:
    needs: release
    if: needs.release.outputs.hasChangesets == 'false'
    runs-on: ubuntu-latest
    outputs:
      to_tag: ${{ steps.publish.outputs.to_tag }}
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
      - name: Assemble + publish vendored plugins, emit registry gap list
        id: publish
        run: |
          to_tag=$(node scripts/publish-vendor.mjs)
          echo "to_tag=$to_tag" >> "$GITHUB_OUTPUT"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  release-vendor:
    needs: [release, publish-vendor]
    if: needs.release.outputs.hasChangesets == 'false'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include: ${{ fromJSON(needs.publish-vendor.outputs.to_tag) }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check tag already exists
        id: tag-exists
        run: |
          if git ls-remote --exit-code --tags origin "refs/tags/${{ matrix.name }}@${{ matrix.version }}" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Create local tag
        if: steps.tag-exists.outputs.exists != 'true'
        run: git tag "${{ matrix.name }}@${{ matrix.version }}"

      - name: Push git tag
        if: steps.tag-exists.outputs.exists != 'true'
        run: git push origin "${{ matrix.name }}@${{ matrix.version }}"

      - name: Check if GitHub Release exists
        id: release-exists
        run: |
          if gh release view "${{ matrix.name }}@${{ matrix.version }}" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Create GitHub Release if missing
        if: steps.release-exists.outputs.exists != 'true'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ matrix.name }}@${{ matrix.version }}
          generate_release_notes: false
          body: |
            Assembled from upstream [${{ matrix.upstreamRepo }}](https://github.com/${{ matrix.upstreamRepo }}) @ `${{ matrix.upstreamTag }}`.

  release-plugin:
    needs: [release, publish-vendor]
    if: needs.release.outputs.hasChangesets == 'false'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: osuperpowers-router
            tag_prefix: "osuperpowers-router@"
          - name: osuperpowers
            tag_prefix: "osuperpowers@"
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive
          fetch-depth: 0
      - name: Read plugin version
        id: ver
        run: echo "version=$(node -p "require('./packages/${{ matrix.name }}/package.json').version")" >> "$GITHUB_OUTPUT"
      - name: Check plugin was versioned in this release
        id: versioned
        run: |
          if [ ! -f .changeset/versioned-plugins.json ]; then
            echo "versioned=false" >> "$GITHUB_OUTPUT"
          elif jq -e --arg p "${{ matrix.name }}" '.[] | select(. == $p)' .changeset/versioned-plugins.json >/dev/null 2>&1; then
            echo "versioned=true" >> "$GITHUB_OUTPUT"
          else
            echo "versioned=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Check tag already exists
        id: tag-exists
        run: |
          if git ls-remote --exit-code --tags origin "refs/tags/${{ matrix.tag_prefix }}${{ steps.ver.outputs.version }}" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Create local tag
        if: steps.versioned.outputs.versioned == 'true' && steps.tag-exists.outputs.exists != 'true'
        run: git tag "${{ matrix.tag_prefix }}${{ steps.ver.outputs.version }}"
      - name: Push git tag
        if: steps.versioned.outputs.versioned == 'true' && steps.tag-exists.outputs.exists != 'true'
        run: git push origin "${{ matrix.tag_prefix }}${{ steps.ver.outputs.version }}"
      - name: Check if GitHub Release exists
        id: release-exists
        if: steps.versioned.outputs.versioned == 'true' && steps.tag-exists.outputs.exists != 'true'
        uses: actions/github-script@v9
        with:
          script: |
            const tag = `${{ matrix.tag_prefix }}${{ steps.ver.outputs.version }}`;
            try {
              await github.rest.repos.getReleaseByTag({ ...context.repo, tag });
              core.setOutput('exists', 'true');
            } catch (e) {
              if (e.status !== 404) throw e;
              core.setOutput('exists', 'false');
            }
      - name: Create GitHub Release if missing
        if: steps.versioned.outputs.versioned == 'true' && steps.tag-exists.outputs.exists != 'true' && steps.release-exists.outputs.exists != 'true'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ matrix.tag_prefix }}${{ steps.ver.outputs.version }}
          generate_release_notes: true

  sync-develop:
    needs: [release, publish-vendor]
    if: needs.release.outputs.hasChangesets == 'false'
    uses: ./.github/workflows/sync-main-to-develop.yml
    secrets: inherit
