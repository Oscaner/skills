# 消费者面一致性（Consumer Parity）P3 实施计划 — 本仓校验面重建
**Spec:** [2026-09-21-consumer-parity-p3-design.md](docs/osuperpowers/specs/2026-09-21-consumer-parity-p3-design.md)

- **Parent program**: [2026-09-21-consumer-parity-overall.md v1.17](../specs/2026-09-21-consumer-parity-overall.md)
- **Depends on**: P2（shipped，PR #272 `39ed65a8`）；P3 design v1.2 Approved（`db48a64e`，spec-review-5，blocker=0）
- **Base**: develop（finishing read-base 数据源；consumer-parity-p3 工作分支）

## Constraints

### 口径

P3 是**本仓校验面重建 phase（允许破坏性变更）**：repo 侧四表守卫整体退役（S1/S2 + 42 用例 + canary-dogfood）· validate 接线净化（零四表 block、steps 12→11、全语义名零编号 anchor）· canonical phase-id 语法严格 A（`^P\d+(\.\d+)*$`）· S3 残留簇改写 · consumer-sim 升级挂 release 门 · skills 输出零过滤 + fix 边界条款 · docs-family 命令出口统一（结果可见性 + exit.ts 出口单源）· 运维规范落档。**判据面唯一实现 = engine lifecycle（dispatch 运行时 + engine 套件）——repo 侧零第二触发点**；engine 判据不动，只删 repo 侧（C1/C3）。所有 engine 改动 `pnpm run validate` 11 块全绿 + `emit:check` 零 drift + engine vitest 全绿。engine 零文档写入（overall 回填由 orchestration 执行，closeout 折叠 S3 改写）。SKILL.md / docs / docs/maintainers 英文主源；本 plan 中文（Strategy B）。**changeset：P3 不建**——版本化发布归 P4（overall §4 输入 = `.changeset ×2`）；P3 对发作面（schema patterns / stdout 结果面 / prepare 移除）随既有 `consumer-parity-p2-major.md` changeset 在 P4 版本化（P4 若判定需补记，P4 任务承载）。

**计划期决策（design 授权 plan 定，记录第一类）**：
- D1（§2.6 fixture 二选一）→ **临时仓内生成**：consumer-sim 从安装面 schema/templates 派生一份合法 plan 写入 mkdtemp（不把 smoke-plan 移入 `templates/`——不向消费者包面塞 engine 测试 fixture；同时实证「schema→authoring」消费者路径）
- D2（§2.9 exit.ts 命名）→ 补 **`exitOkWith(resultLine: string): never`**（成功 + stdout 结果面一次调用）；result 面 line 由共享 helper 构建；docs-family review/fix 完成三处出口统一
- D3（AC4 探针二选一）→ **前缀锚定探针 + node 断言**落 `ci-validate.test.mjs`（探针 `(?:^| )\b(?:[0-9]+\.|5b\d*|5c|8-10)[. ]+[A-Za-z(]` 对 step-name 面零残留 + 现名全命中/语义名全 miss 防白绿）
- D4（§2.6 prepare 移除的接线变化）→ engine vitest 增 `globalSetup`（`dist/cli.mjs` 缺则显式 `dev:stub`，独立面自给；validate 5b0 已覆盖 CI 面）

### commit 边界机制

dispatch 两端门——入口门（进入 review 前工作树干净）+ 出口门（产生修改的 dispatch 后修改已提交）。跨任务连续性：T1 删除守卫与 index.ts/pre-commit.ts 接线改写**同任务原子化**（validate 每任务末保持全绿，中间不落红）；T4 schema pattern 迁移与 tokens/documents 消费迁移同任务闭环（engine 套件同任务末全绿）；T5/T8 编辑 SKILL.md/docs 后同任务内 `pnpm run emit` 重生成 + `emit:check`（emit 产物是派生物，禁止手改）。**overall 回填（v1.18）** = branch-review 前置义务（overall v1.12 时序模型）——T8 完成后才进 branch-review/finishing；S3 中 overall 四处改写折入同一回填。

### Flow Atomicity

八任务线性链、逐任务可独立验证：T1（守卫退役 + 接线净化）→ T2（42 行逐案对照表落盘）→ T3（canary-dogfood 删除）→ T4（phase-id 语法 A）→ T5（skills 零过滤 + fix 边界条款）→ T6（docs-family 出口统一）→ T7（consumer-sim）→ T8（S3 残留改写 + 运维规范落档 + 全链收口）。每任务引擎测试就近落（`src/**/__tests__/**/*.test.ts`）；任务间不放跨任务悬空项（发现即回填 spec/overall 再继续）。

### 顺序原则

守卫删除与接线同任务（避免 validate 中间红）；42 行对照表在守卫删除后从 `git show HEAD:` 读取（文件已删，映射内容不变）；phase-id 语法 A 的 schema 迁移先于消费迁移（pattern 单源先行，tokens/documents 从新 pattern 派生，杜绝两代 token 并存）；skill/doc 面编辑先 emit 后验 drift；docs-family 出口统一（T6）先于运维规范第 8 项落档（T8）——结果面与出口实现在场才可落档（design §2.7 sequencing）；T7 的 `prepare` 移除先于 release.yml 插桩（consumer-sim 前置）；T8 = 收口（overall 回填 + 全量复核），此后仅剩 branch-review/finishing。每步 validate 保持全绿。

---

### Task 1: S1/S2 守卫退役 + validate 接线净化（design §2.1/§2.4 · AC1/AC4）

- **Do**: ① **steps 语义名全改名**（按 design §2.4 表，仅改 `name` 字段不动 run 逻辑）——`emit-check.ts:18` `0. unified emit freshness (emit-check)` → `emit freshness (checked against regenerated products)`；`osuperpowers.ts:34` `5b. osuperpowers plugin validation` → `osuperpowers plugin resolution`；`:68` `5b. osuperpowers skills-count` → `osuperpowers skills inventory count`；`:77` `5b. node:test behavior` → `osuperpowers node:test behavior tree`；`:82` `5b. wiring guard: ci-validate.test.mjs` → `validate wiring guard (ci-validate.test.mjs)`；`engine.ts:20` `5b0. cdd-engine stub materialization (dev:stub)` → `cdd-engine dev stub materialization`；`:26` `5b1. cdd-engine Vitest engine suite` → `cdd-engine engine test suite (vitest)`；`residue.ts:1354` `5c. engine zero-residue + channel-audit grep` → `engine zero residue + channel audit`；`marketplace.ts:165` `6. marketplace validate` → `marketplace manifests validate`；`lib-tests.ts:15` `7. scripts unit tests (vitest)` → `scripts unit tests (vitest)`；`version-sync.ts:65` `8-10. version sync` → `package version sync`。② **整体删除** `scripts/validate/overall-consistency.ts`（543 行）、`scripts/validate/plan-spec-anchors.ts`（314 行）、`scripts/validate/__tests__/{overall-consistency,plan-spec-anchors}.test.ts`（27 + 15 用例）。③ **接线改写**——`index.ts`：删 `import { steps as overallConsistencySteps } from "./overall-consistency.ts"`（:19）+ `...overallConsistencySteps`（:37）+ 顶部注释「Composes the 12 per-block」→ 11 + interleave 注释去「12 names」；`pre-commit.ts`：删 `import overallConsistencySteps` + `...overallConsistencySteps` + 注释「12-block full suite」→ 11（残留 6 组 11 块 = emit(1) + osuperpowers(4) + residue(1) + marketplace(1) + scripts-unit(1) + version-sync(1)）。④ **钉死面同步**——`packages/osuperpowers/tests/ci-validate.test.mjs`：`:49/:72` `s.name === "5b. osuperpowers plugin validation"` → `osuperpowers plugin resolution`；`:69` `name.startsWith("5b. node:test")` → `osuperpowers node:test behavior tree`；`:77/:103` `startsWith("5b1. cdd-engine Vitest")` → `cdd-engine engine test suite (vitest)`；`:108/:121` `startsWith("5c.")` → `engine zero residue + channel audit`；`:161`「12. overall consistency step present」用例删（改断言 `steps.some(s => s.name === "12. overall consistency")` 为 false——无四表 block）；`:168` `assert.equal(steps.length, 12)` → `11`。`scripts/validate/__tests__/pre-commit.test.ts`：`expect(fullSteps).toHaveLength(12)` → `11`；组成覆盖表 `"5b. osuperpowers plugin validation"/"5c. engine zero-residue…"/"6. marketplace validate"/"7. scripts unit tests (vitest)"/"8-10. version sync"/"12. overall consistency"` → 语义名六项（`osuperpowers plugin resolution` / `osuperpowers skills inventory count` / `osuperpowers node:test behavior tree` / `validate wiring guard (ci-validate.test.mjs)` / `engine zero residue + channel audit` / `marketplace manifests validate` / `scripts unit tests (vitest)` / `package version sync`，删 `12. overall consistency`——8 位语义名，含 4 件 osuperpowers + 5c + 6 + 7 + 8-10 面）；subset 排除断言 `startsWith("5b0.")"/"startsWith("5b1.")` → 语义名 `cdd-engine dev stub materialization` / `cdd-engine engine test suite (vitest)`。⑤ **AC4 探针（D3，node 断言面）**——`ci-validate.test.mjs` 增用例：探针 `/(?:^| )\b(?:[0-9]+\.|5b\d*|5c|8-10)[. ]+[A-Za-z(]/` 对 `steps.map(s => s.name)` **全零命中**（前缀锚定/整串判定——数字前缀步名全捕获、语义名零误报）；**防白绿 node 断言**——旧名逐个 `assert.match(name, probe)` 全 HIT（`0. unified emit freshness (emit-check)` / `6. marketplace validate` / `7. scripts unit tests` / `12. overall consistency` / `5b` 族 / `5c` / `8-10` 族），新语义名逐个 `assert.doesNotMatch(name, probe)` 全 MISS（`emit freshness (checked against regenerated products)` / `package version sync` / `osuperpowers plugin resolution` / `cdd-engine dev stub materialization` / `engine zero residue + channel audit` 等）。⑥ **F8a 盲区闭合**——守卫文件删除后 `grep -rn "\bH1\b" scripts/validate/` 仅余 residue.ts / residue.test.ts（守卫自身定义/测试）——`scripts/` 面不再持有 charter 退役词 H1 的使用（不扩 ALL_MECH_POSITIONS）。
- **验收**: `grep -rE "overall-consistency|plan-spec-anchors" scripts/` 零命中（`-E` 交替，非 BRE 字面 `|`）；`index.ts`/`pre-commit.ts` 无 `overallConsistencySteps` import；steps 全语义名（`grep -rn "\"5b\.\|0\. unified\|12\. overall" scripts/validate/*.ts` 零命中类探针绿）；ci-validate.test.mjs / pre-commit.test.ts 钉死值 11 且无旧名断言；AC4 探针 node 断言绿（现名全命中 / 语义名全 miss）；F8a 实证（`\bH1\b` 仅守卫自身）；`pnpm run validate` 全绿（11 块）· engine vitest 不受影响。
- **注**: 42 用例内容在 T2 借 `git show HEAD:` 恢复，不依赖本任务保留；11 语义 = emit(1)+osuperpowers(4)+engine(2)+residue(1)+marketplace(1)+scripts-unit(1)+version-sync(1)。

### Task 2: 42 行逐案对照表落盘（design §2.1 · AC1 后半）

- **Do**: ① **42 条 repo 断言逐案恢复**——`git show HEAD:scripts/validate/__tests__/overall-consistency.test.ts`（27 条）与 `git show HEAD:scripts/validate/__tests__/plan-spec-anchors.test.ts`（15 条），按 `it("…")`/`test("…")` 标题逐条列案。② **逐案映射 engine 对应用例**——每条 repo 断言 → engine 对应用例 `文件:行` + 覆盖判定（同判据面已由 engine 测试覆盖 → 删，零功能损失）。映射面 = design §2.1「唯一实现面」枚举：`rules/__tests__/documents.test.ts`（face ①–⑥ + row-shape + version ascending + split-phase）· `doc-contract-channels.test.ts`（dispatch 级四表 BLOCK）· `closeout.test.ts`（终态欠账 + 声明源→列双向）· `lifecycle-validation.test.ts`（lineage 触发 / 自审边界）。③ **落盘**——42 行 markdown 表附于本 design §2.1 之后（`## Appendix: 42-row cross-reference` 或同位置命名；列：repo case · engine case（file:line）· verdict），表格单元格内 `|` 按 `\|` 转义（`splitCells` 约定）；design §2.1「落盘时点 = P3 plan T2」句同步改「✔ 已落盘（P3 T2）」，AC1 收口复核由本任务验收承接。
- **验收**: 对照表 42 行 = 27 + 15 无缺漏（行数与标题枚举对账）；每行含 engine 对应用例（file:line 存在性抽查——`sed -n` 复验行号）+ 覆盖判定；无「判据面未覆盖需迁入」行（有则立即回填 design 再判——预期全「判据面已覆盖 → 删」）；`grep -rE "overall-consistency|plan-spec-anchors" scripts/` 仍零命中；`pnpm run validate` 全绿；AC1 复核成立（删面零功能损失实证可复现）。
- **注**: 对照表是 AC1 的逐案论证物（「并入或删除」→ 全删零迁入的判据证据），不改变任何执行面；映射以 engine 测试自造链语义为准，不要求 repo 断言与 engine 用例逐字对应。

### Task 3: canary-dogfood.test.ts 删除 + engine 套件零产物 fixture 实证（design §2.2 · AC2）

- **Do**: ① 删除 `packages/cdd-engine/src/rules/__tests__/canary-dogfood.test.ts` 整文件（79 行，硬编码 `docs/osuperpowers/specs/2026-09-21-consumer-parity-*.md` 三条真实路径为 fixture——产物删即测试废的耦合病）。② **零产物 fixture 实证**——`grep -r "2026-09-21-consumer-parity" packages/cdd-engine/src/**/__tests__/` 零命中；排除口径复查：`docs/osuperpowers` **布局字符串**不属产物引用（14 文件以 `docs/osuperpowers/specs/...` 相对路径在 mkdtemp 临时仓复刻消费者等效布局——handoff-naming / progress-owner / cdd / lifecycle-validation / docs-runner / plan-constraints / doc-contract-channels / helpers / runner / root / infra.root / templates.cache / closeout-fixtures / documents——合成 fixture 合法保留，design §2.2 排除口径明文）。③ engine vitest 全绿（`pnpm --filter @oscaner-skills/cdd-engine test`——既有自造链用例不依赖该文件）。
- **验收**: 文件从工作树消失（`git show HEAD:packages/cdd-engine/src/rules/__tests__/canary-dogfood.test.ts` 存在、工作树无）；grep `2026-09-21-consumer-parity packages/cdd-engine/src/**/__tests__/` 零命中；engine vitest 全绿；`pnpm run validate` 全绿。
- **注**: canary 实证续存形态 = **程序运行期**（本 P3 plan → p3 design → overall 文档链经 P3 全程 dispatch 被 engine 审计零失败——P3 acceptance 字面）；本任务**不新增任何读本仓产物的测试**。

### Task 4: canonical phase-id 语法严格 A（design §2.3 · AC3）

- **Do**: ① **schema 单点迁移**——`packages/cdd-engine/src/documents/schema/overall.json` 全部 8 处 phase-id 承载 pattern 迁移 `^P\d+(\.\d+)*$`：`:109` `issueInventory.row.phaseId` → `^P\d+(\.\d+)*$`；`:165` `phaseInventory.rowShape.idFormat` → `^P\d+(\.\d+)*$`；`:201` `cells.dependency` → `^P\d+(\.\d+)*\s*->|^P\d+(\.\d+)*\s*->\s*\(?\s*soft\s*\)?`（保持 soft 形状）；`:214` `hardEdge` → `^P\d+(\.\d+)*\s*->\s*P\d+(\.\d+)*$`；`:219` `softEdge` → `^P\d+(\.\d+)*\s*->\s*\(?\s*soft\s*\)?\s*P\d+(\.\d+)*$`；`:314` `claimPatterns.designToken` → `^P\d+(\.\d+)*-design$`；`:323` `phaseReference.single` → `^P\d+(\.\d+)*$`；`:328` `phaseReference.range` → single 派生 `\s*[-–—]\s*` 连接（`^P\d+(\.\d+)*\s*[-–—]\s*P\d+(\.\d+)*$`）。**description 全写**——phaseId description 按 design §2.3 原文（canonical form `P<digits>(.digits)*` 例 `P1`·`P2.1`·`P2.2.3`；点分数字表示子 phase 层级，`P2.1` 是 `P2` 的子 phase；层级语义 = 拆分/收敛操作的注册单位；字母后缀 `P2a` 与连字符 `P2-1` 不合法；排序 (major, segments…) 自然序）；range/designToken/idFormat/dependency-cell/hardEdge/softEdge 同源补注（同一 phase-id 形式，语义随承载面：dependency cell = 边 token）。**不留旧语法并存**（C2——1–5 处死 pattern 实测判定：全 engine 消费面已从 schema 派生，无独立字面第二处）。② **engine 消费面改造**——`tokens.ts deriveDocTokens`：phaseIdScanRe 构造链去 `[a-z]?` 字面（原 `:277` `.replace(/\[a-z\]\?/, "")` 随 pattern 消失自然失效）、digit-run 捕获 `(\d+)` → `(\d+(?:\.\d+)*)`（`P2.1` 数字脊整体捕获）；claimPhaseRefToken（single/range）· phaseTokenScanRe · designTokenScanRe · designDocTail 承接新 pattern（segment-join 语义自动成立：`P2.1-design` 归属 P2.1 非 P2）；`documents.ts`：`ownDesignToken`（`:735` 现 `DESIGN_TOKEN_RE.source.replace("\\d+(?![0-9])[a-z]?", suffix)` → 改 `"\\d+(?:\\.\\d+)*"` 替换、`P2.1-design` owns P2.1）· `phaseIdFromPlan`（`:386` `P${m[1].slice(1)}` → segment 感知 `…-p2.1.md` → `P2.1`）· 相关注释语汇（`:93/:381/:394/:732/:733/:764/:766` 的 `P1a` 表述）同步语义化。③ **测试改造（split-phase 族全量 `P2.1` 化）**——`documents.test.ts`：`:304` face ① claim 用例（`P2.1` verbatim 不 collapse 为 `P2`）、`:399` face ④ `…-p1a.md` → `…-p2.1.md`、`:317-329` 链（`P1a` 行 + dependency 边 `P1a -> P2` → `P2.1 -> P2`）、`:411/:440/:447` 行/cell/claim；`tokens.test.ts:157-162` claimSinglePhaseRe/claimPhaseRangeRe 断言改 `P2.1` / `P2.1–P2.3`（捕获组 `["P2.1","2.1"]`，数字脊含点）。④ **新用例（语法初衷面 = 子 phase 依赖图）**——dependency cell `P2.1 -> P2.2`（cells.dependency 迁移断言）· hardEdge `P2.1 -> P2.2` · softEdge `P2.1 -> (soft) P2.2` · range `P2.1–P2.3` 端点包含 · `P2.1-design` 归属 · `…-p2.1.md` glob——落 `documents.test.ts` / `tokens.test.ts`。
- **验收**: canonical overall.json 全 8 处 pattern 迁移 + description 全写（`grep -n "P\\\\d+(?![0-9])\[a-z\]\?" packages/cdd-engine/src/documents/schema/overall.json` 零命中——旧语法并存禁止）；engine 消费面（tokens/documents）从新 pattern 派生、零硬编码第二处；split-phase 测试全量 `P2.1` 化 + 依赖边新用例绿；**本仓存量文档零迁移**（overall P1–P4 · 各 phase spec/plan 头全 bare——过 engine 审计零新增失败，canary：P1/P2/p3 文档链 dispatch 审计零误伤）；`pnpm run validate` 全绿 + engine vitest 全绿。
- **注**: 本任务同时是 AC3「engine 套件含语法新用例」承载位；`P1a`/`P2a` 字面从 schema/tokens 灭绝后，`grep -rn "P1a\|P2a" packages/cdd-engine/src` 仅余注释级残留（本任务随改）。

### Task 5: skills 调用 cdd 输出零过滤 + fix 边界条款（design §2.8/§2.2 · AC8）

- **Do**: ① **零过滤表述落地**——全部编排 skill（`cli-driven-development` / `writing-overall-spec` / `writing-phase-spec` / `writing-single-spec` / `writing-plans` / `brainstorming` / `finishing` / `report-issues`，八件 `packages/osuperpowers/skills/*/SKILL.md`）凡含 cdd 调用指引的节点增「**direct invocation — read the full stdout/stderr; cdd truncates its own output**（no `tail`/`head`/`2>&1 |`/`EXIT=$?` wrappers）」指引；现存含「capture」「last block」类表述若指过滤则删改。② **fix 边界条款（defect②修复）**——`cli-driven-development`（fix-task / fix-branch）+ `writing-overall-spec` / `writing-phase-spec` / `writing-single-spec`（fix-spec）+ `writing-plans`（fix-plan），五节点 Do 补「review 后编排者**只读 status / blocker 计数**（stdout result 面，§2.9）；findings 全文由 `cdd fix` 的 fix-agent 经 `--findings <handoff>` 消费；编排者**不得**自行应用 findings as inline edits」——消除「读了 findings 自己改」湿滑斜坡。③ **emit 重生成**——SKILL.md 是 emit 源：改后 `pnpm run emit` + `pnpm run emit:check` 零 drift（`.claude-plugin/` / `.cursor-plugin/` / `marketplace/` / `.github/ISSUE_TEMPLATE/` 派生物随改）。
- **验收**: AC8 grep 零命中——`grep -rE "cdd[^\"']{0,40}(tail|head|EXIT=|2>&1[[:space:]]*\|)" packages/osuperpowers/skills/*/SKILL.md` = 0；八个 SKILL.md 的 cdd 调用节点含直接调用表述（抽查：含 cdd 调用指引的节点数 ≥ 「read the full output」表述计数）；五件 fix 节点含 fix 边界条款措辞（grep「只读 status」或等价）；`pnpm run emit` 后 `emit:check` 零 drift；`pnpm run validate` 全绿。
- **注**: scope 限定 skill 指引面（SKILL.md 散文 + 节点 Do 措辞）；repo 侧 `scripts/`（run.ts/smoke-cdd）自身调用不受此规约约束（脚本内部 pipe 是程序逻辑，非指引）；不新增 CLI；rules 面（engine 黑盒）不涉。

### Task 6: docs-family 命令出口统一 + exit.ts 出口单源（design §2.9 · AC9）

- **Do**: ① **exit.ts 补统一出口（D2）**——`packages/cdd-engine/src/infra/exit.ts` 增 `export function exitOkWith(resultLine: string): never`（`process.stdout.write(resultLine + "\n")` + `throw new ExitRequested(0)` 语义——成功 + stdout 结果面一次调用，屏弃「先 write 后 exit」分散写法）；exit table 0/1/2/3 不变。② **result 面构建与接线**——共享 helper（`src/cli/result-face.ts`）`docsResultFace(result: { exitCode: number; handoff: Record<string, unknown> | null }, handoffPath: string): string` → `status: <s> · blocker: <n> · handoff: <path>`（`<s>` = handoff.status；`<n>` = handoff.blocker 计数；`<path>` = 本地 handoffPath）；`cli/review.ts` docs review 完成（`:151` `runDocsTask`）改捕获返回值 → `:170` 裸 `return;` 处打印 result 面 + 出口（`result.exitCode === 0` → `exitOkWith(face)`；非 0 → `process.stdout.write(face + "\n")` 后 `exitWithCode(result.exitCode)`）；`cli/fix.ts` task fix 完成（`:48` 裸 `return;`）→ `exitOk()`（task 通道 return-block 已有 stdout 契约，无需重复 face）；`cli/fix.ts` docs fix 尾（`:101` `runDocsTask` 后）→ 同样捕获结果、打印 face + exit.ts 出口（docs fix 完成现 stdout 零 result 面）。③ **cdd.test.ts docs-family 断言面同步**——`:121` dry-run review --type spec 用例：`expect(r.stdout).toMatch(/status: APPROVED/)` + `toMatch(/· blocker: 0/)` + `toMatch(/· handoff:/)`；沿用既有 dry-run fix --type task return-block 断言对齐 fix 面；若加 docs-fix 结果面断言 → 借 `cdd.test.ts` 既有 docs 面 dry-run 通道。④ **grep 实证**——`grep -rn "^\s*return;$" packages/cdd-engine/src/cli/*.ts` = 0（命令级裸 return 零命中，dispatch 层控制流 return 保持）；cli 层出口调用面 = exit.ts 族单源（grep：cli/*.ts 不再直接 `throw new ExitRequested` / `process.exit`）。
- **验收**: AC9——docs-family review/fix 完成 stdout 含 result 面（`status:` 行可 grep；cdd.test.ts docs-family 断言面已同步更新）；`cli/*.ts` 命令级裸 return 零命中；exit.ts 族 = cli 层唯一出口调用面（grep 实证）；exit table 0/1/2/3 不变；engine vitest 全绿 + `pnpm run validate` 全绿。
- **注**: 本任务修复 defect①（docs-family stdout 零结果面——编排者被迫翻 handoff 文件的湿滑斜坡起点）+ 为 defect② 的编排侧可读面提供机制（编排者读 status/blocker 行即可 routing，findings 详情仍留 handoff 由 `cdd fix --findings` 消费）。

### Task 7: consumer-sim（smoke-cdd 升级 · 挂 release 门）（design §2.6 · AC6）

- **Do**: ① **`prepare` 钩子移除**——`packages/cdd-engine/package.json` `scripts` 删 `"prepare": "pnpm run dev:stub"`（裸 `pnpm pack`/`pnpm publish` 不再触碰 dist——发布品 = 显式 build 门的真实产物；`pnpm install` 后不再自动桩化 dist）。② **接线变化登记（D4）**——engine vitest cli 黑盒用例（cdd.test.ts `CDD_MJS = packages/cdd-engine/dist/cli.mjs:20`，spawn dist）在 prepare 移除后不再自动桩化：`packages/cdd-engine/vitest.config.mjs` 增 `globalSetup`（`dist/cli.mjs` 缺失时 `execaSync pnpm -C packages/cdd-engine dev:stub`——独立面自给；validate 5b0 已先行覆盖 CI 面）+ vitest.config 注释登记（本地单跑 cli 黑盒测试：显式 dev:stub 或 globalSetup 兜底）。③ **smoke-cdd.ts 重写 = consumer-sim**——`scripts/validate/smoke-cdd.ts`（157 行）按 design §2.6 六步重写：(1) `pnpm --filter @oscaner-skills/cdd-engine build`（真实 unbuild 产物入 dist，实测 71.7 kB）；(2) 包目录 `cd packages/cdd-engine && pnpm pack --pack-destination <out>`（`worktree` 包目录运行——prepare 已移除，备防加 `--config.ignore-scripts=true`）；(3) **tarball 内容断言（防白绿）**——`dist/cli.mjs` grep stub 标识 `createJiti` / `node_modules/.pnpm` 零命中 **且** 字节数 > 10 kB（桩 614 B / 真实 71.7 kB——禁用「>100 kB」阈值防误判真实产物）；`templates/` 与 `dist/documents/schema/`（canonical overall/plan/phase-spec/add-phase-protocol）在场；(4) mkdtemp 临时仓 `npm install <tarball>`（消费者布局，零仓内路径依赖；不读 `docs/osuperpowers/`、不引用本仓 node_modules——`dist/` = build 门生成的发布品机制，非前提假设）；(5) 消费者链——安装链入口实证（`node <installed>/dist/cli.mjs` 或 `node_modules/.bin/cdd`）+ `cdd help`（CLI 绝对目录 = `<installed>/dist/cli.mjs`；schema 目录可寻址 = `<installed>/dist/documents/schema/` + `<installed>/templates/schema/`）+ **5-command dry-run 链**（implement / review task / fix task / review branch / fix branch）——**fixture plan 临时仓内生成（D1）**：从 tarball 内置 schema/templates 派生一份合法 plan 写入临时仓（`# <title>` + `**Spec:**` + `**Parent program**`（若需）+ `### Task 1:` + `- **验收**:` + `## Constraints`（Form-A）——满足 plan-schema 解析，零本仓路径）；(6) 逐命令断言 return-block 契约（`status` / `commits` / `artifacts` / `blocker` / `counters`）输出消费者等效结果。入口子命令 `node scripts/run.ts smoke-cdd` 保留同名、语义 = consumer-sim（不新增子命令——Non-goal#1 尊重）。④ **release 门挂点**——`.github/workflows/release.yml` `release` job 内、`changesets/action` 之前插 `- run: node scripts/run.ts smoke-cdd`（发布动作前黑盒跑消费者链）；`pr-validate.yml:17` 既有 step 保留（快反馈），复核其在重写后仍绿。
- **验收**: AC6——`prepare` 已移除（package.json `grep -n prepare` 零命中）；`pnpm pack` 后 tarball 与工作树 dist 均保持真实产物（tarball 内 stub 标识零命中 + 字节实证）；consumer-sim PR 面 / release 门可跑（`node scripts/run.ts smoke-cdd` 全绿——pack → install → 消费者链全绿 + tarball 断言 + fixture plan 临时仓生成 + 零仓内路径）；`cdd help` 安装面 schema 目录可寻址实测；engine vitest 全绿（globalSetup 兜底）+ `pnpm run validate` 全绿。
- **注**: 失效机制登记（防再采白绿）——`pnpm pack --ignore-scripts` 旗标不受支持（Unknown option）· `npm_config_ignore_scripts=1` env 无效 · workspace root 相对路径 pack 被解析为 registry spec（`ERR_PNPM_PACKAGE_VERSION_NOT_FOUND`）——pack 必须从包目录运行；`--config.ignore-scripts=true` 为实证有效备防；隔离兜底 = 从临时克隆执行 pack（零工作树残留）；旧 smoke-cdd 的 P5 残留面 sweep（sync `collectGateLexiconHits`）与 validate 5c（residue.ts 面）重合一面——consumer-sim 专注消费者链 + tarball 断言，残留面职责归 5c，不双写 sweep。

### Task 8: S3 残留改写 + 运维规范落档 + 全链收口（design §2.5/§2.7 · AC5/AC7/AC10）

- **Do**: ① **S3 残留簇改写（engine 执法位 / 历史时态）**——overall 四处（cross-cutting「本程序自身四表仍受现有 `scripts/validate/overall-consistency.ts` 机器校验，直至 P3 退役该守卫」→ 「本仓文档合规 = engine lifecycle 审计（dispatch 运行时 + engine 套件），无 repo 侧守卫」；Issue inventory P3 行 / Phase inventory P3 scope cell 同改写；Maintenance「本程序自身四表在本轮程序中仍受…」→ 历史时态 + engine 位）+ `docs/maintainers/osuperpowers-plugin.md:116-120`（「The one charter-level guard in the set is `overall-consistency`…maintainer-mode, this-repo dogfood」段 → 引擎执法位表述 + 历史时态：「守卫已于 P3 退役；charter 判据面 = engine lifecycle audit + engine 套件」）+ `CLAUDE.md:47`（pre-commit 组成「…/ 12 overall consistency」→ 11 项语义名清单「0 emit freshness / 5b osuperpowers 树 + wiring guard / 5c residue + channel-audit / 6 marketplace / 7 scripts unit / 8-10 version sync」语义名化删 12）+ `writing-overall-spec/SKILL.md:54`（现时态「A repo-local `scripts/validate` guard is maintainer-mode dogfood only, not a consumer surface」→ 历史时态 + engine 位：「本仓文档合规判定与消费者同引擎路径——engine lifecycle audit + engine 套件（canary 实证运行期）；repo-local `scripts/validate` guard 已于 P3 退役」）——SKILL.md 改后 `pnpm run emit`。② **运维规范落档（八项）**——`docs/maintainers/program-experience.md` 增 consumer-parity 区段（新 section，英文主源 Strategy B extension、可 grep Verify）：**1 零产物 fixture**（unit/e2e = 功能性验证，不允许以本仓产物路径为测试 fixture；canary 实证归运行期 dispatch）· **2 零编号 anchor**（validate step 名 = 语义名，退役 5b0/5b1/12. 族）· **3 零 legacy 豁免死代码**（C3 命中即删）· **4 phase-id 语法 A**（`P<digits>(.digits)*` 点分分层、弃字母/连字符）· **5 唯一执法面**（四表判据 = engine lifecycle，repo 侧零第二触发点）· **6 validate 11 块结构快照**· **7 cdd 输出零过滤**（禁 tail/head/2>&1/EXIT= 包装；cdd 已自身优化输出长度）· **8 docs-family 结果可见性 + 命令出口 exit.ts 单源**（第 8 项 sequencing：T6 实现在场后本任务写入——本任务为收口位）。③ **overall v1.18 回填（branch-review 前置义务）**——four-table sync：P3 Design-spec 列 `[Pending]→p3-design v1.2`、Implementation plan 列 `[Pending]→Done`、change-history v1.18 行（守卫退役 + 接线 11 块 + 零编号 anchor + 语法 A + consumer-sim + 出口统一 + 落档八项）、version bump；本任务 ① 的 overall 四处改写随此回填落笔。④ **全量收口复核**——`pnpm run validate` 11 块全绿（干净已提交树）· `pnpm run emit:check` 零 drift · `pnpm run precommit` 绿灯 · engine vitest 全绿 · AC1–AC10 逐条复核清单（交付时逐条列证据）。
- **验收**: AC5——overall 四处 + osuperpowers-plugin.md + CLAUDE.md + SKILL:54 改写为 engine 执法位/历史时态；`grep "scripts/validate.*guard\|block 12.*consisten"` shipped docs/skills 面零现行主张（历史时态句允许）；AC7——program-experience 八项条目英文主源、各自可 grep Verify（第 8 项于本任务（T6 后）写入）；AC10——validate 11 全绿 · emit:check 零 drift · precommit 11 绿灯 · engine vitest 全绿 · AC1–AC9 逐条复核清单交付；overall v1.18 四表一致性（change-history 行与列态互证）。
- **注**: 本任务是收口位（S3 改写 + 落档 + 回填同任务——overall 回填 = branch-review 前置义务，完成后才可 branch-review/finishing）；canary 运行期实证 = 本 plan → p3 design → overall v1.17/1.18 整链经 P3 全程 dispatch（T1–T8 review/fix）审计零失败。