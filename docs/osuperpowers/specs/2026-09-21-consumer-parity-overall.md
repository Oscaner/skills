# 消费者面一致性（Consumer Parity）— Overall Spec

- **Version**: v1.37 · 2026-09-25
- **Status**: Approved
- **Author**: [human] · Claude Opus 5 (1M context) (osuperpowers:brainstorming)
- **Constraints**:
  - 仓库语言政策：SKILL.md / docs 英文主源；本 spec 中文（Strategy B internal docs）
  - 不 commit 除非用户明确要求；spec 交付除外（writing-overall-spec I2 立即提交）
  - changeset 逐 phase 建
  - **允许破坏性变更**（engine breaking 面，2026-09-21 用户拍板）
  - 开发期引擎调用：`node packages/cdd-engine/dist/cli.mjs` 直调，不走 global register

## Document scope

Charter only — no implementation detail.
- **Overall approval is not equivalent to any phase started**（GATE）。
- 变更先回填本 overall（backfill-as-version），再继续实现。

## File paths

| Artifact | Path |
|---|---|
| Overall | `specs/2026-09-21-consumer-parity-overall.md` |
| Phase spec | `specs/2026-09-21-consumer-parity-p<N>-design.md` |
| Phase plan | `plans/2026-09-21-consumer-parity-p<N>.md` |

## Program charter

**Goal**：消除本仓使用 osuperpowers（skills + 插件）+ cdd-engine 与消费者安装使用产品之间的行为分歧——文档/charter 合法性的执法位归位到 **engine lifecycle**（docContractValidate / statusValidate 全自动，零新增执法子命令，唯一例外 = `cdd schema get` 发现型（`cdd help` 子命令 v1.32 整体移除）），本仓自己的程序与消费者走同一执法路径，本仓 = 产品缺陷首发 canary，行为校验面收敛到消费者等价面，发布的内容即被校验的内容。

四表纪律（charter 合法性面；docContractValidate 全量审计 + closeout 统一规则，与 P1 triage / P2 全量审计逐项对应）——「四表」＝ 双向 backfill 声明 ↔ 列、plan/design 文档存在性、依赖图成员、锚点注册域（锚点 ∈ overall 文档 Issue inventory 表行，无锚 no-op）四张表状执法面（下述第 1/2/3/5 条）；phase 注册完整性（第 4 条）与 issue 行 well-formed（第 6 条）随必要子集并入全量审计与 engine 判据面，不单列、不计入四表：
1. 双向 backfill 声明 ↔ 列
2. plan/design 文档存在性
3. 依赖图成员
4. phase 注册完整性（随必要子集并入全量审计，不单列）
5. 锚点注册域——锚点 ∈ overall 文档 Issue inventory 表行；无锚 no-op（engine 通用自证）
6. issue 行 well-formed（并入 engine 判据面）
7. closeout 统一规则：声明源 ↔ 列双向全列（含 engine 派生终态并入声明源）；**时序模型（v1.12，2026-09-21 用户裁决——撤销 v1.11 lane 边界）**：**回填 = branch-review 前置义务**——plan complete（engine 终态）后、branch-review 前，orchestration 必须完成 backfill-overall（version bump + change-history claim + 四表列回填；finishing **撤销**回填机制，仅存 merge/PR 决策 + close-issues）；**终态欠账硬门**——branch-review（含一切 plan-bearing dispatch）在「其 resolve 的 parent overall 下存在 plan-complete 未回填终态」时 pre-flight BLOCKED + 指引（先 backfill-overall）；post-flight plan-complete 且未回填 → 高亮回填 recommand（回填由 orchestration 执行）；结构性面（缺失 cell / 缺 claim）mismatch 非空 → BLOCK + 指引（不变）；v1.11 lane 边界（branch 排除终态成员）**一笔勾销**——branch-review 前提 = plan 已结束 → 回填义务已到期 → 拦 = 正确行为

**Non-goals**：
- **不新增 cdd CLI 子命令**（2026-09-21 用户拍板，执法走 lifecycle 自动执行；**唯一例外 = `cdd schema get <type>`**——发现型信息子命令：`cdd schema get <doc-type>` 直出 canonical schema 原文（doc-type 枚举随 engine `DOC_SCHEMA_NAMES` 全量四件：overall · plan · phase-spec · add-phase-protocol；2026-09-24 P4.3 用户裁决确立，废除「help 定位目录 + Read 文件」的跨系统查找面）；`cdd help` 子命令 v1.32 **整体移除**（其发现面唯消费者 read-schema 已全改 schema get 直取，cli/templates/schemas 三行目录面全空消费者；`--help` 旗标 = citty usage 渲染面，非子命令、不受豁免清单约束）；**零执法逻辑**，除外仍零新增子命令）
- 不改 emit / marketplace / changeset 内部流水（范围边界：产品面 + 校验面）——v1.10 收窄：emit 对 doc-structure 的参与面**归零**（canonical = engine 包内 JSON Schema 随包发布、skills 直取成文，emit 不渲染 doc-structure 模板、无 md 模板产物；v1.2 同源派生表述据此修订，2026-09-21 grilling 裁决）；marketplace / changeset 流水仍不动
- 不把本仓 GitHub issue 注册数据语义强加给消费者（锚点注册域并入 engine 通用自证、无锚 no-op；本仓只保留 issue **数据**本体，不再是本仓侧检查）
- README / CLAUDE.md **全面重写**由 P4.1 承担（含 **README.zh-CN.md = 同步 mirror**、repo 对外宣讲面——与主源同承零分歧保证；2026-09-23 P4 brainstorm 裁决，废除本节旧限制）：写入内容覆盖全部宣称面（harness 支持面 · 安装/来源宣称 · 行为描述类陈述），重写后与落地行为零分歧；是整文档重写（弃现有章节分布与内容），非行为描述类增量

**Cross-cutting constraints**：
- 破坏性变更允许；从高维度统一抽象，确保最佳实践，**不留技术债务**（必要子集透镜并入全量审计，无双实现；必要子集——即现 docContractValidate 的 necessary 子集校验（rules/documents.ts；overhaul v1.58 落地）——作为全量审计的必要成员并入，不单列）
- **spec/plan 结构定义同源派生**（用户 2026-09-21 要求，沿 data-driven-template 约定）：doc-structure 单源 = engine 包内 **canonical JSON Schema + descriptions**（v1.10：schema 唯一结构事实）→ skills 经 **`cdd schema get <type>` 直取 schema 成文（v1.28 起，doc-structure 内容面唯经 `cdd schema get`；`cdd help` 目录发现面——`schemas:` 行 v1.30 删、子命令整体 v1.32 移除——唯消费者 read-schema 已全切 schema get，死面即删；`--help` 旗标 usage 面保留）** + engine 侧 docContractValidate / brief 抽取**同构运行时消费**——零双维护、零 md 模板副本；emit 对 doc-structure 参与面归零（无 emit:check 该面守卫，见 Non-goal #2）；运行时组合校验兜底
- engine 零文档写入保持——引擎只判只指引，回填由作者执行
- 本仓文档合规 = engine lifecycle 审计（dispatch 运行时 + engine 套件），无 repo 侧守卫
- **判据定式（三判据；P1 逐项 triage 与 P2/P3 处置的记在案判定规则）**：**C1 可达性**——一条 charter 合规断言，只要消费者环境（纯包 + 无 `scripts/`、无可安装 validate）也应得到同等执法 → 归 engine lifecycle（docContractValidate / statusValidate）；engine 无法承载其时机（与 dispatch 无关的结构约束）→ 归 data-driven-template 单源或产品文档；落 repo scripts 侧第二实现 = 违规。**C2 结构性**——断言对象是结构 token / heading / 模板 → 一律同源派生（d4t 单源），engine 运行时同构消费；repo/skill 侧任何 md 模板副本全禁，**schema 为唯一结构事实**（2026-09-21 grilling 裁决）。**C3 退化（canary）**——本仓任何「仅本仓侧 charter 执法」依赖 → 处置二选一：并入 engine（该断言值得消费者得到）或删除（仅本仓必要而消费者不需要 ⇒ 本仓也不该有）；useless 必删、不留历史叙述豁免（2026-09-21 用户裁决）；处置完成态 = validate 全绿且零 scripts 侧兜底引用

## Issue inventory

| Phase | Issue (ref) | Title summary |
|---|---|---|
| P1 | none | 分歧面逐项 triage 判定 + shim 排查（triage 判据与 shim 处置明细落盘 P1 design 判定表） |
| P2 | none | engine lifecycle 统一抽象：docContractValidate 全量审计 + statusValidate 结案强制一致（breaking） |
| P2 | none | docs-family fix 通道 commit 义务未 concretize（2026-09-21 dogfood 实证：同契约两轮异行为 spec-fix r1 BLOCKED / spec-fix r2 APPROVED）——P2 harness 契约统一必清项 |
| P3 | none | 本仓校验面重建：overall-consistency / plan-spec-anchors 退役 + validate 接线走 engine 同路径 + smoke-cdd 升级 consumer-sim 挂 release 门 |
| P4.1 | none | 文档治理：charter 修订承接（Non-goal #4 正向约束 v1.19 基线已改正；P4.1 落笔期 v-bump + 四表同步）· docs/maintainers 重组 + 文件编号 · README.md/CLAUDE.md/README.zh-CN.md 全面重写（zh-CN 同步 mirror） |
| P4.2 | none | 发布一致性闭环：×9 changesets 版本化整合 + breaking 发布面（cdd-engine 1.0.0 首次稳定开版）+ consumer-sim release 门实测 + pack 内容面审计（osuperpowers files 面）+ 宣讲面收尾（README 增 cdd + osuperpowers 理念篇幅，尤其 cdd 理念 · docs/maintainers 运维文档精炼，太多太杂）+ repo 定位改述「基于 cdd 理念的方法论」+ gh 更新 GitHub 元信息（v1.33 仅记录 · 待 P4.2 启动分析） |
| P4.3 | none | cdd 多 task 模式（`--task` → `--tasks`：`--tasks 1` / `--tasks 1,2`）+ cli-driven-development loop 前 task groups 裁定节点 + Mid-Flight Backfill I4 语义歧义修复（任何 dispatch 返回即落地 · 暂停循环闭合） |
| P4.3 | #274 | doc-contract plan 列「实施中」中间态（[In-flight]）+ claim target 等值口径 + 子句 prose 误命中 claim 目标诊断 |
| P4.3 | #276 | overall.json 描述与 doc-contract enforcement 三处不一致（表头 / issue-ref / Phase 行形状）+ 报错不附正确形态 |
| P4.3 | none | cdd schema get 发现型子命令（直出 canonical schema，省去跨系统文件查找心智负担）+ skills read-schema 直取更新 |
| P4.4 | none | scripts/ 与 cdd-engine 全面 OOP 化：P4.3 TaskGroup 单数据模型扩展为全层类抽象 + scripts 编排面同构，空壳/死代码即删，破坏性变更于 1.0.0 开版前收口（scope/AC 于其 brainstorm 细析）+ biomejs 全面接入（husky pre-commit + lint） |

## Phase inventory

| # | Phase | Scope | Design spec | Implementation plan | Acceptance criteria | Dependency |
|---|---|---|---|---|---|---|
| P1 | **分歧面审计 + 判定登记**：判据驱动逐项 triage——① 双向 backfill / ② 文档存在性 glob / ③ 依赖图成员 → **engine**；④ phase 注册完整性 → 随必要子集并入全量审计，不单列；⑤ 锚点注册域（锚点 ∈ overall 文档 Issue inventory 表行）→ **engine 通用自证**（无锚 no-op）；⑥ issue 行 well-formed → 并入 engine 判据面；**shim 排查**——本仓现有绕产品面的短路面逐项清点、逐项给处置结论（能去掉的去、能并入 engine 的并——C3 退化判据：useless 必删、不留历史叙述豁免）；13 件 overhaul 族冻结文档「仅本仓侧 charter 执法」引用即刻清理（验收② A 裁决：frozen 照清，file:line 逐处处置映射——改写中性句 / 剥除主张 / 删句，见 P1 design 处置表） | [p1-design v1.1](2026-09-21-consumer-parity-p1-design.md) | Done | P1 design 落盘 = triage 判定表 + shim 清单（逐项处置结论）；本仓程序文档（2026-09-13-osuperpowers-overhaul 族——overall 及其 p1–p6 design / plan 共 13 件）无任何"仅本仓侧 charter 执法"依赖残留（13 件清理后 grep 零活主张，block 12 自检 2/2 仍绿）；处置 shim 产生的文字/结构改动经 cdd 链 review；S5（run.ts:89 stale「(4-command H1 chain)」描述）单行修正纳入本 phase | 无（program 起点） |
| P2 | **engine lifecycle 统一抽象（breaking）**：(1) **全量 charter 审计**（docContractValidate pre-flight）——**lineage 驱动触发**：dispatch 文档链（plan → Spec 头解析 → Parent program 头解析 → overall）resolve 出 parent overall → 整程序四表一次齐查（双向 backfill 声明 ↔ 列、文档存在性、依赖图成员、锚点注册域——锚点 ∈ overall 文档 Issue inventory 表行（文档表，非 GitHub issue 数据），无锚 no-op、phase 注册完整性、issue 行 well-formed——2026-09-21 grilling：phase 注册检查不再依赖 basename P 编号命名）；resolve 不出 → 四表审计 no-op、necessary-subset 恒跑；**全通道**——docContractValidate / statusValidate 提升 base 默认 override（task/docs/branch 全生效；overall 自身为 review 对象时自审）；(2) **closeout 统一规则**（2026-09-21 grilling 替代 F13 机械封死表述）——声明源 ↔ 列**双向全列**（plan + design：列已完成 ⇒ change-history 有 match claim；claim ⇒ 列非 [Pending]），**engine 派生终态（plan-complete）并入声明源**；mismatch 集**单一推断模块**；**pre-flight 硬门**——结构性面（缺失 cell / 缺 claim）mismatch 非空 → BLOCK + 逐项指引（dry-run CDD_WARN、退出码语义不变）；**终态欠账硬门（v1.12 用户裁决，撤销 v1.11 lane 边界）**——plan-complete 未回填（终态源 = 该 parent overall 下所有 plan workspace）→ plan-bearing dispatch（含 branch-review）pre-flight BLOCKED + 指引「先 backfill-overall」（**回填 = branch-review 前置义务**）；**post-flight 高亮 recommand**（plan-complete 且未回填 → 打印下一步回填，可 diff 列缺口）**同源消费**；回填由 orchestration 在 branch-review 前执行（finishing 撤销回填机制，非后置）；necessary 子集透镜并入全量审计（rules/documents.ts 重构、无双实现）；(3) **spec/plan 结构定义同源派生**（2026-09-21 grilling 裁决）——canonical = engine 包内 **JSON Schema + descriptions**（不转 md）；skills 经 **`cdd help` 发现通道**（打印 CLI 绝对目录 + 必要文档目录）**直取 schema 成文**（零硬编码路径）；engine 侧 docContractValidate / brief 抽取同构消费同一 schema（删硬编码 token 与 SKILL.md 散文 token 的双手写）；存量 md 模板（overall / phase-spec / add-phase-protocol）随 schema 落地**退役**（模板/文档执法位迁移：add-phase-protocol 等标注 lifecycle 执法位，移除“机械守卫在本仓 scripts 侧”的表述）；(4) **harness 契约单源**（2026-09-21 grilling 升级）——task/docs/branch 共享**一个 lifecycle 契约**：handoff schema 核心块统一（commits.base/head · status · exit-gate 判据 · round-context base token），lane 差异仅边界物（doc_path vs task 序号）；逆转 docs-handoff-schema 既有“无 commits 字段”显式声明；出口门判据不变 + BLOCKED 面补可见诊断输出（spec-fix r1 BLOCKED「uncommitted at return」/ spec-fix r2 APPROVED 同契约异行为消除，2026-09-21 dogfood 实证）；breaking 版本面 = major | [p2-design v1.4](2026-09-21-consumer-parity-p2-design.md) | Done | 任意 dispatch（implement/review/fix/docs）在 parent overall 四表不合法时给 BLOCKED + 指引（退出码语义不变；dry-run CDD_WARN）；closeout 声明源↔列双向全列（含 engine 派生终态并入声明源）一致性——结构性面（缺失 cell / 缺 claim）pre-flight mismatch 非空 → BLOCK + 指引；**终态欠账（plan-complete 未回填）**——plan-bearing dispatch（含 branch-review）pre-flight BLOCKED + 指引、post-flight 高亮回填 recommand（退出码语义不变；**回填 = branch-review 前置义务**、finishing 撤销回填机制——v1.12 用户裁决）；本仓与消费者的 charter 执法同一条 engine 路径（无 scripts 侧兜底依赖）；引擎测试覆盖全量审计 + closeout 统一推断 + 契约确定性三新面；breaking 版本面明确（handoff schema + round-context + lifecycle 契约结构升级 = cdd-engine major）；**doc-structure 单源实证——改 canonical 定义一处 → engine 校验/抽取同步生效 + skills 消费同源产物，grep 零 md 模板副本、零第二处手工 token**；**`cdd help` 落地面——打印 CLI 绝对目录 + 必要文档目录正确、零执法逻辑、skills 零硬编码路径**；**docs-family 契约确定性实证——同契约束轮次行为一致（提交 → APPROVED；未提交 → BLOCKED + stdout 可见诊断）**；**零债断言——无 plan-only 遗留 · closeout 推断无第二实现 · 无豁免例外常量 · handoff schema 无双核心块 · base 默认 override 全通道生效** | P1 ->(hard) |
| P3 | **本仓校验面重建**（2026-09-22 grilling 合流，v1.14 修正三处；v1.15 补 skills 输出零过滤；v1.16 补命令出口统一）：`scripts/validate/overall-consistency.ts` + `plan-spec-anchors.ts` **退役**（42 用例 **全删零迁入**——判据面 engine 测试已用 mkdtemp 自造链覆盖；P3 design 落 42 行逐案对照表：repo 断言 → engine 对应用例:行 → 覆盖判定）；**接线重构为零接线**——四表判据面唯一实现 = engine lifecycle（docContractValidate），本仓运行期每步 dispatch 自身被审（canary 实证回归运行期形态）、测试面 = engine 5b1 套件自造链；**`canary-dogfood.test.ts` 删除**（产物耦合病：硬编码本仓真实文档路径为 fixture，产物删即测试废——unit/e2e 测试 = 功能性验证，不允许以本仓产物为 fixture，2026-09-22 用户裁决）；validate **无四表 block**（steps 12 → 11，ci-validate.test.mjs / pre-commit.test.ts 钉死值同步）；**steps 全语义名零编号 anchor**（`5b0/5b1/5b/12. overall consistency` 族退役——validate 接线重构时整名语义化，无意义 anchor 不利运维，2026-09-22 用户裁决）；**canonical phase-id 语法严格 A**（`^P\d+(\.\d+)*$`——点分数字分层，弃用字母后缀；canonical schema 全部 8 处 phase-id 承载 pattern（phaseId · idFormat · cells.dependency · hardEdge · softEdge · designToken · single · range）单点派生 + **schema description 写明语法与分层语义**，2026-09-22 用户裁决）；S3 活文档残留簇改写（overall 四条 · osuperpowers-plugin.md:116-120 · CLAUDE.md:47 · overall-spec SKILL:54 → 守卫退役后的 engine 执法位表述）；F8a `\bH1\b` 盲区随守卫文件删除自消（不扩 ALL_MECH_POSITIONS；守卫自身定义/测试除外）；`smoke-cdd` 升级 consumer-sim（**先 build 后 pack**——真实 unbuild 产物入 tarball → 临时仓安装 → 消费者视角跑引擎链）挂 release 门；**skills 调用 cdd 输出零过滤**（禁止 `tail`/`head`/`2>&1 \|`/`EXIT=$?` 包装——cdd 已自身优化输出长度，2026-09-22 用户裁决补充）+ **fix 边界条款**（review 后编排者只读 status/blocker 计数、findings 全文由 `cdd fix` fix-agent 消费、不得 inline 应用 findings——defect②修复）；**docs-family 命令出口统一**（docs review 完成后 stdout 结果面 status/blocker/handoff + `cli/*.ts` 命令级出口禁止裸 `return;`、统一走 `exit.ts` 出口族——defect①修复，2026-09-22 用户裁决）；**运维规范落档**（零产物 fixture · 零编号 anchor · 零 legacy 豁免死代码 · phase-id 语法 A · cdd 输出零过滤 · 命令出口 exit.ts 统一 —— 2026-09-22 grilling 裁决同步进 docs/maintainers） | [p3-design v1.2](2026-09-21-consumer-parity-p3-design.md) | Done | `scripts/validate/` 无 charter 守卫残留（grep/sweep 实证 + 42 行逐案对照表，grep `-rE` 零残留）；case 删面 = 零功能损失（逐案对照 + engine 测试面覆盖实证）；validate 无四表 block、steps 全语义名零编号 anchor（step name 面 `^[0-9][0-9.\-bc]*[. ]+[A-Za-z(]` 前缀锚定族零残留（v1.18 归一：v1.17 的 `\|` 转义形态照字面复制为验证 grep 即白绿——已换本无 pipe 形态，验证 grep 显式带 `-E` 可直接拷贝（`grep -E "^[0-9][0-9.\-bc]*[. ]+[A-Za-z(]"`——BRE 默认下 `+` 为字面量，缺 `-E` 照字面复制即零命中白绿，恰为本归一欲消除的形态；确定性 fallback = ci-validate.test.mjs 的 node 断言探针，或直接运行该断言面）；还原步骤登记 v1.18 行，且 v1.17 change-history 行探针已同步归一）——数字前缀步名全捕获、语义名零误报，node 断言「现名全命中 / 语义名全 miss」防白绿；旧式 `[0-9]+[. ]`+尾 `\b` 失效：尾 `\b` 在「.」→「 」non-word→non-word 边界断、吞点后备选项失配，`12. overall consistency`/`0. unified emit freshness (emit-check)`/`6. marketplace validate`/`7. scripts unit tests` 均漏检）；`canary-dogfood.test.ts` 删除后 engine 套件零本仓程序产物 fixture 引用（grep `2026-09-21-consumer-parity` 零命中；`docs/osuperpowers` 布局字符串 = 消费者等效布局合法复现，排除）；canonical phase-id 语法 A 落地（8 pattern 单点派生 + description 全写 + engine 消费面零误伤、split-phase 测试改造、依赖边用例）；本仓程序文档链经 P3 全程 dispatch 审计零失败（canary 运行期实证）；consumer-sim 黑盒在发布门可跑并输出消费者等效结果（tarball = 真实 build 产物）；skills cdd 调用零过滤（`packages/osuperpowers/skills/*/SKILL.md` grep `(cdd\|cdd-engine).*(tail\|head\|EXIT=\|2>&1 \|)` 零命中 + direct-invocation 表述齐备）+ fix 节点边界条款落地；docs-family review stdout 结果面（`status:` 行可 grep）+ `cli/*.ts` 命令级裸 return 零命中（`grep "\^\s*return;$"` = 0）+ `exit.ts` 出口族 cli 层单源（exit table 0/1/2/3 不变）；`pnpm run validate` 重构后全绿（11 块）；运维规范落档（docs/maintainers，2026-09-22 裁决八项） | P2 ->(hard) |
| P4.1 | **文档治理**（2026-09-23 P4 brainstorm 拆分裁决）：① **charter 修订承接**——Non-goal #4 正向约束 v1.19 基线已改正（重写后的 README/CLAUDE.md 之 harness 支持面 / 安装 / 来源宣称 / 行为描述类陈述与落地行为零分歧）；P4.1 落笔期再 v-bump + 四表回填（backfill-as-version）；② **docs/maintainers/** 重新整理（2026-09-23 用户裁决：重新整理重新编号，**包括精简与删除**，纠正「内容保真迁移」口径）——精简冗余/死内容、`07-osuperpowers-plugin.md` 删除（发布流程由 `.changeset/README.md` 承载）、编号重排连续（English-primary 保持）；③ **README.md / CLAUDE.md 全面重写**（弃现有章节分布与内容），**mirror 集 = 根 + 两包三件（2026-09-23 用户裁决 mirror 扩展，p4.1-design §1/§3）**——根 `README.zh-CN.md` = 同步 mirror（repo 对外宣讲面，活同步）+ `packages/osuperpowers/README.zh-CN.md` / `packages/cdd-engine/README.zh-CN.md`（包级宣讲面），全仓 `.zh-CN.md` 三件零其他 | [p4.1-design v1.5](2026-09-21-consumer-parity-p4.1-design.md) | Done | charter 四表同步（基线 v1.19；backfill-as-version 于 P4.1 程内完成）；docs/maintainers 重组后文件编号 + 索引干净、零互链断裂（全族 11 处链接迁移完整）；README/CLAUDE.md/README.zh-CN 重写后与落地行为零分歧（行为描述 + harness 宣称面全校验）；语言政策按本程序修订执行（全英文主源；mirror 集 = 根 + 两包三件 `README.zh-CN.md`——2026-09-23 用户裁决扩展，修订随 CLAUDE.md 重写落笔，见 P4.1 ③ / p4.1-design §1/§3）；英文 README 语言切换行维持 `[中文]`（2026-09-24 用户裁决：language switch 不用改——`[Simplified Chinese]` 改向撤销回滚）；docs/maintainers 精简整理实证（编号连续 · `07-osuperpowers-plugin.md` 删除 · live 引用零残留，frozen 豁免） | P3 ->(hard) |
| P4.2 | **发布一致性闭环**（原 P4 发布面 + 版本基准整备，2026-09-23 拆分）：旧程序 osuperpowers-overhaul ×9 changesets 版本化整合（发布顺序裁决 = 一次集成发版 · backlog-* 6 件占位符清除 · cdd-engine 声明值 `1.0.0 → 0.1.0` 基准降值 · `consumer-parity-p2-major.md` 声称改写 · version-sync 块补 cdd-engine 覆盖）；breaking 发布面——cdd-engine **1.0.0 首次稳定开版**（P2 breaking 收进 changelog，不起跳 2.0.0）、osuperpowers → 0.2.0；consumer-sim release 门实测通过（发布品即校验品）；**pack 内容面审计**（npm pack 含 dist/templates/skills，不含仓内 tests/、scripts/ 治理残件——osuperpowers 现整树进包，补 files 白名单）；README/CLAUDE.md 一致性交由 P4.1 重写承接；**理念宣讲篇幅（2026-09-24 用户裁决）**——README 增 **cdd + osuperpowers 理念介绍**（尤其 cdd 理念：cdd 是什么 · 为什么这样设计 · 三种模式链 · Review Convergence 等收敛纪律的产品理念导览；P4.1 为行为一致性重写，P4.2 补充理念宣讲增量）；**docs/maintainers 运维文档精炼（2026-09-24 用户裁决）**——现有运维文档体量太大太杂，收尾收敛（删除冗余 / 合并碎篇 / 精简篇幅，P4.1 重组基础上的进一步精炼）；**repo 定位改述 cdd 方法论（2026-09-24 用户裁决 · v1.33 仅记录待 P4.2 启动分析）**——repo 不再定位「个人 AI 编程技能市场」，改述「基于 cdd 理念的方法论」（README 家族 + CLAUDE.md 定位 + 宣讲面，v1.31 理念宣讲增量承接），并用 gh 更新 GitHub 仓库元信息（description / topics / homepage，与改述定位一致） | [Pending] | [Pending] | 发布的 cdd-engine 1.0.0 / osuperpowers 0.2.0 经 consumer-sim 实测通过（发布品即校验品）；pack 内容面审计干净（osuperpowers tarball 零 tests/、scripts/ 治理残件）；README/CLAUDE.md 与落地行为零分歧（行为描述 + 宣称面由 P4.1 重写全量保证）；**理念宣讲落地实证——README 含 cdd 理念导览章节（cdd 是什么 · 设计动机 · 模式链 · 收敛纪律，grep 断言）+ osuperpowers 理念导览**；**运维文档精炼实证——docs/maintainers 体积/篇数收敛（删冗余、合碎篇、精简篇幅，收敛前后对照）+ 互链完整零断裂**；两程序 changesets 版本化无冲突、变更面归属清晰；版本基准整备无流程 gap（0.1.0 降值 + 声称改写 + backlog 清除 + version-sync 覆盖全落地） | P4.4 ->(hard) |
| P4.3 | **cdd 多 task 模式 + task groups 裁定 + Mid-Flight Backfill 语义修复**：① cdd CLI `--task` → `--tasks` 多 task 模式（`--tasks 1` · `--tasks 1,2`＝单数据模型：`--tasks` 列表（长度 ≥ 1）即唯一 dispatch 面，`--tasks 1` 为长度 1 的组，无单/多双面；分组后全走组（review 一轮审整组 · fix 整组重申）——2026-09-24 grilling 裁决）② cli-driven-development 正式 enter loop 前新增 **task groups 裁定**节点（task groups 界定 → 用户确认 → 才进 implement→review→fix loop；分组规划落盘 phase plan「Task Groups」节、默认全单组、调整独立 commit——2026-09-24 grilling 裁决）③ **Mid-Flight Backfill 语义歧义修复**——writing-* 族 SKILL.md 的 I4 文本补明确析：任何 dispatch（implement/review/fix）返回即落地 backfill · **暂停**循环闭合 · 独立 commit · 干净树后恢复循环；涉 engine CLI + `skills/`（emit 输入面——需 `pnpm run emit` + 产物重生成，核 cli-driven-development 与四件 writing-* SKILL 措辞一致——I4 文本承载面 = 五件：writing-single-spec / writing-overall-spec / writing-phase-spec / writing-plans + cli-driven-development） ④ **doc-contract plan 列中间态修复（#274，2026-09-24 用户裁决并入）**——plan 列新增 `[In-flight]` 中间态（`isInflightText`：已开工 · 无 reverse claim 义务）+ claim target 等值口径放宽（link 形态 plan cell 对齐 design `ownDesignToken` 提取、link 指向同一 plan 文档即等）+ 子句内 prose 误命中 claim 目标诊断改进（closeout 规则新合法态：`[Pending]` → `[In-flight]` → `**Done**`） ⑤ **overall.json 描述↔enforcement 对齐（#276，2026-09-24 用户裁决并入）**——schema 描述层三处改向 enforcement 实读形态（change-history 表头首格 `version` · issue-ref 合法枚举 none/#NNN/[#NNN]/#issuecomment-<digits>/整格括号 · Phase 行 6 内容列 c1=id/c3=Design/c4=plan/c6=dependency）+ 报错附 `should look like:` 正确形态（bad/empty version · unrecognized issue ref · not a Phase-inventory id 三处） ⑥ **`cdd schema get` 发现型子命令 + skills read-schema 直取**（2026-09-24 用户裁决纳入）——新增 `cdd schema get <overall, phase-spec, plan>` 直出 canonical schema 原文（stdout · 零执法逻辑）；skills（writing-* read-schema 节点）从「`cdd help` → 目录 → Read 文件」改「`cdd schema get` 直取成文」，砍跨系统文件查找心智负担；Non-goal #1 例外由 `cdd help` 扩为 `cdd help` + `cdd schema get` 双发现型（charter v1.28 修订）；skills 文本属 emit 输入面随改重生成 · `cdd help` 子命令整体移除（v1.32 用户裁决：schema get + read-schema 直取后 help 三行发现面全空消费者——`schemas:` 行 v1.30 删、`cli:`/`templates:` 两行 v1.32 连根删 help.ts/bin 拦截/parse 声明/help 测试；smoke-cdd 三行断言改锚 `cdd schema get plan`（AC6 安装面 addressability 同形替换）；Non-goal #1 双发现型缩为单发现型 `cdd schema get`；`--help` 旗标 usage 面保留） ⑦ **skill-anatomy 骨架 schema（2026-09-25 用户裁决 · plan reopen 加 Task 11）**——节点锚定式 SKILL.md 结构契约 canonical JSON Schema（`DOC_SCHEMA_NAMES` 五件：overall · plan · phase-spec · add-phase-protocol · skill-anatomy），validate 机器校验 8 件 SKILL.md，**严格 allowlist**（注册表外章节/标题 → BLOCK——skill 是消费者真实入口面，零不确定性；演进历史/非消费者指导写入 = 缺陷），descriptions 承载规则（替代 skill-authoring 文本指导，后者全清空 `git rm`） | [p4.3-design v1.7](2026-09-21-consumer-parity-p4.3-design.md) | Done | `--tasks` 单/多 task 语义正确（`--tasks 1` 与 `--tasks 1,2` dispatch 行为实证 + engine 测试绿）；task groups 裁定节点在 cli-driven-development flow digraph 中存在且 loop 入口以用户确认门控；Mid-Flight Backfill I4 文本五面（writing-single-spec · writing-overall-spec · writing-phase-spec · writing-plans + cli-driven-development）一致、语义无歧义（grep + 措辞核对）；`pnpm run emit` 后 emit:check 无 drift；`[In-flight]` 计划列状态合法（isInflightText · 无 reverse claim 义务）+ link 形态 plan cell 与 claim 双向等值（ownDesignToken 对齐）+ 子句 prose 误命中改进实证；overall.json 描述与 enforcement 三处同形（对拍断言）+ 三处报错附 `should look like:` 正确形态；`cdd schema get <overall, phase-spec, plan>` 输出与 canonical schema 同字节（stdout 直出）、未知 doc-type exit 2 + 可用名枚举、零执法逻辑；三件 schema-bearing 技能（writing-overall-spec / writing-phase-spec / writing-plans）read-schema 节点全改 `cdd schema get <type>`（writing-single-spec read-schema 显式 N/A、无 canonical 结构 schema）、零残留 `cdd help` 定位串（grep 断言）+ emit:check 无 drift；`cdd help` 子命令整体移除（`cdd help` 引擎面 + docs/maintainers + 跨面 grep 零命中、help.ts/bin/parse/help.test 四文件删除；`--help` 旗标 usage 面不回归）；smoke-cdd 改锚 `cdd schema get plan` 后 AC6 安装面 addressability 实测过；Non-goal #1 单发现型（`cdd schema get`）修订落地（其余零新增不变）；`pnpm run validate` 11 块全绿 | P4.1 ->(hard) |
| P4.4 | **scripts/ 与 cdd-engine 全面 OOP 化**（2026-09-24 P4.3 brainstorm 用户裁决注册，先注册不探索）：统一抽象升级——P4.3 的 TaskGroup 单数据模型（`--tasks` 列表即唯一 dispatch 单位）扩展为 cdd-engine 全层类抽象（TaskLifecycle · round-context · handoff · 进度 · residue 一致化，零标量/过程式残面）+ `scripts/` 编排面（run.ts · validate）同构 OOP 化；空壳、死代码即删（无过渡 shim · 无别名 · 无历史叙述豁免）；破坏性变更于 P4.2 1.0.0 首次稳定开版前收口；**biomejs 全面接入（2026-09-24 用户裁决）**——husky pre-commit 钩子 + biome lint/format 全面落地（src + scripts + 全仓 ts 面），静态质量门与既有 precommit 校验链并列；具体 scope/acceptance 于本 phase brainstorm 阶段细析 | [Pending] | [Pending] | 具体验收准则于本 phase brainstorm 定稿（charter 注册期从略）；落地后 `pnpm run validate` 11 块全绿；biomejs husky + lint 全面接入实证（pre-commit 钩子触发 biome lint，零违规通过；lint 配置随仓发布） | P4.3 ->(hard) |

## Dependency graph (ASCII)

```
P1 -> P2   (hard: P2 落地以 P1 判定表为输入)
P2 -> P3   (hard: 校验面重建依赖引擎新执法面)
P3 -> P4.1 (hard: 文档治理承接 P3 收口后的文档面——零守卫时代的维护文档重组)
P4.1 -> P4.3 (hard: 多 task 模式与 task groups 裁定落地于 P4.1 收口后的文档/直调链路)
P4.3 -> P4.4 (hard: 全面 OOP 化承接 P4.3 的 TaskGroup 单数据模型——TaskGroup 值对象扩展为 engine/scripts 全层类抽象)
P4.4 -> P4.2 (hard: 发布闭环验收面依赖 P4.4 落地——1.0.0 首次稳定开版消费 OOP 化完成态与 --tasks/flow 修订)
```

Legend:
- `->` = hard block（依赖前置 phase 发布后方可启动）
- `-> (soft)` = suggestion only（非阻塞排序建议；本图无边）

## Boundary rules

> 每 phase：完整 brainstorm → plan → dev。Shipped before dependents start。
> Dev 期发现的新 needs / 新约束 MUST 先回填本 overall（version bump + change-history 行 + sync affected phase acceptance/dependency）再继续实现，不允许实现期绕过。

## Maintenance

- 四表逐 phase 回填；charter only，无 task 列表（phase spec 承载增量）
- 本仓文档合规判据面 = **engine lifecycle**（dispatch 运行时审计 + engine 套件自造链；canary 实证归运行期 dispatch）——repo 侧 `scripts/validate` charter 守卫已于 P3 退役（此前它机器校验本仓四表，属「程序终结于其目标——dogfood」形态，随本轮收口删除）
- 策略偏移 / phase 拆分即时回填本 overall

## Change history

| v1.0 | 2026-09-21 | 程序 charter：消费者面一致性——charter 执法位归位 engine lifecycle（零新增 CLI）、仓库侧守卫退役、校验面 = 消费者等价面、发布一致闭环（P1–P4） | [human] · Claude Opus 5 (1M context) |
| v1.1 | 2026-09-21 | cdd spec-review r1（blocker=0，3 warn + 5 nit）全 finding 落地：⑥ 并入 P2 全量审计枚举、④ 随必要子集并入不单列、Non-goal#4 行为描述/宣称类拆分、F13 内联 gloss、Goal 拆句 + 纪律枚举、issue 行单行化、P1 验收枚举 2026-09-13-overhaul 族、依赖图补图例（v1.0→v1.1） | [human] · Claude Opus 5 (1M context) |
| v1.2 | 2026-09-21 | 用户增补：spec/plan **结构定义同源派生**（data-driven-template 约定）——canonical doc-structure 单源 → skill templates 渲染产物 + engine 校验/抽取同构消费，零双维护；P2 scope/acceptance + cross-cutting 增补（v1.1→v1.2） | [human] · Claude Opus 5 (1M context) |
| v1.3 | 2026-09-21 | cdd spec-review r2（1 warn + 1 nit）全 finding 落地：Non-goal #2 显式 carve-out（emit 内部流水改动仅限 doc-structure 模板渲染目标 + 其 emit:check / 运行时组合接线，属产品面 + 校验面承载，marketplace / changeset 流水仍不动）；锚点注册域措辞统一——纪律 #5 / P1 ⑤ / P2 全量审计同用「锚点 ∈ overall 文档 Issue inventory 表行；无锚 no-op」，P2 枚举加注 inventory 指文档表非 GitHub issue 数据（v1.2→v1.3） | [human] · Claude Opus 5 (1M context) |
| v1.4 | 2026-09-21 | dogfood 发现登记（本仓 canary 首发命中）：docs-family fix 通道 **commit 义务未 concretize**——round-context 无 TASK_FIXED_POINT + docs handoff schema 声言无 commits 字段，出口门却强制 clean tree → 同契约两轮异行为（spec-fix r1 BLOCKED「uncommitted changes at return (fix)」/ spec-fix r2 APPROVED；task-family 对照全 APPROVED）；BLOCKED 面 CLI 无可见诊断。P2 scope/acceptance 增补 harness 契约统一（v1.3→v1.4） | [human] · Claude Opus 5 (1M context) |
| v1.5 | 2026-09-21 | cdd spec-review r3（1 warn + 1 nit）全 finding 落地：「四表」定义锚补枚举 gloss（纪律 lead-in——双向 backfill 声明 ↔ 列 / plan·design 文档存在性 / 依赖图成员 / 锚点注册域，即下述 1/2/3/5 条；phase 注册完整性 / issue 行 well-formed 并入不单列、不计入四表）；docs-family fix 轮次缩写统一改标「spec-fix rN」——v1.4 行 / Issue inventory / P2 scope 三处同改，与既有「spec-review rN」措辞区分（v1.4→v1.5） | [human] · Claude Opus 5 (1M context) |
| v1.6 | 2026-09-21 | 程序批准：Status Draft → Approved（overall 已合入 develop，PR #269）· 进入 P1 brainstorm（v1.5→v1.6） | [human] · Claude Opus 5 (1M context) |
| v1.7 | 2026-09-21 | P1 brainstorm R1–R3 裁决回填：判据定式落盘——C1 可达性 / C2 结构性 / C3 退化（useless 必删、无历史叙述豁免）；shim 清单四分类（替换执法 · 声称残留 · 路径差异 · 描述漂移）+ 处置表 S1–S6；13 件 overhaul 族冻结文档清理 = P1 执行（验收② A 解释：frozen 照清）；处置项新增 S5（run.ts:89 stale「4-command」描述 → P1 单行修正）、S6（.changeset ×2 block-12 声称 → P4 版本化时清）（v1.6→v1.7） | [human] · Claude Opus 5 (1M context) |
| v1.8 | 2026-09-21 | 判据定式落点补全（P1 design r1 review F3 回填）：C1/C2/C3 操作性定义从 change-history 名称式提升为 Cross-cutting 完整定义段——C1 可达性 / C2 结构 token 单源 / C3 退化处置，三判据即 P1 triage 与 P2/P3 处置的记在案判定规则（v1.7→v1.8） | [human] · Claude Opus 5 (1M context) |
| v1.9 | 2026-09-21 | **P1 shipped**（PR #270 合并，`89e5421f`）：P1 design v1.1 Approved（triage 判定表 + shim 清单 S1–S6 + 13 件处置表 26 命中/8 文件）· P1 plan 3-tasks 全闭环（J1 改写 4 处中性化 + J2/J3 保留 22 行实证 + S5 run.ts 单行 + AC 收口）· branch-review APPROVED → branch-fix；canary 实证登记：docs-family fix commit 义务缺口三连复现（P2 必清）、F8a `\bH1\b` 守卫扫面盲区（scripts/ 不在 ALL_MECH_POSITIONS，本 P1 清残留 5 处）；P1 Implementation plan 列回填（[Pending]→Done）；P1 Design-spec 列回填（[Pending]→p1-design v1.1）（v1.8→v1.9） | [human] · Claude Opus 5 (1M context) |
| v1.10 | 2026-09-21 | **P2 brainstorm 裁决回填**（R1–R4 全闭 · phase-size fit 单 phase）：① Non-goal#1 carve-out——`cdd help` 唯一新增子命令（发现型：打印 CLI 绝对目录 + 必要文档目录，零执法逻辑）② Non-goal#2 收窄——emit 对 doc-structure 参与面归零（schema 随 engine 包发布、skills 直取，无 md 模板产物）③ 判据 C2 守卫语扩展——repo/skill 侧 md 模板副本全禁，schema 唯一结构事实 ④ P2 scope C 段改写——canonical JSON Schema + descriptions、`cdd help` 发现通道、存量 md 模板退役 ⑤ P2 scope B 段/E2 合成——closeout 统一规则（声明源↔列双向全列 + engine 派生终态并入声明源 + mismatch 单一推断 + pre-flight 硬门 + post-flight 高亮 recommand 同源消费 + 回填由 orchestration 执行），替代 F13 机械封死表述 ⑥ P2 scope D/acceptance 升级——harness 契约单源（lifecycle 契约核心块统一 + docContractValidate/statusValidate base 默认 override 全通道 + breaking major）+ 零债断言 + `cdd help` 落地面；P2 全量审计触发改 lineage 驱动（v1.9→v1.10） | [human] · Claude Opus 5 (1M context) |
| v1.11 | 2026-09-21 | **P2 spec-review r2 裁决回填（closeout lane 边界）**：二门面判读落盘——结构性面（缺失 cell / 缺 claim）pre-flight mismatch 非空 → BLOCK + 逐项指引；终态欠账面（plan-complete 未回填）pre-flight fail-open、post-flight 高亮回填 recommand（回填由 orchestration 执行）；lane 边界：branch 通道 pre-flight 排除终态欠账成员（其闭环 dispatch 恒位于 finishing 回填之前、非合法咬合点——时序语义推导，非 lane 豁免常量）——纪律 #7 + P2 scope（2）+ P2 acceptance 三处同步（v1.10→v1.11） | [human] · Claude Opus 5 (1M context) |
| v1.12 | 2026-09-21 | **P2 时序模型修订（用户裁决，撤销 v1.11 lane 边界）**：回填 = **branch-review 前置义务**——plan complete 后、branch-review 前 orchestration 完成 backfill-overall（v-bump + claim + 四表列）；finishing **撤销**回填机制（仅存 merge/PR 决策 + close-issues）；**终态欠账硬门**——branch-review（含一切 plan-bearing dispatch）在 parent overall 下存在未回填终态时 pre-flight BLOCKED + 指引；post-flight 高亮回填 recommand 保留；结构性面硬门不变；branch 通道不再排除终态欠账成员（branch-review 前提 = plan 已结束 → 义务已到期 → 拦 = 正确行为）——纪律 #7 + P2 scope（2）+ P2 acceptance 三处同步（v1.11→v1.12） | [human] · Claude Opus 5 (1M context) |
| v1.13 | 2026-09-22 | **P2 closeout 回填（backfill-overall · branch-review 前置义务——engine 终态欠账硬门在 T6 fix dispatch 时 BLOCK，本回填解锁）**：P2 Design-spec 列回填（[Pending]→p2-design v1.4）；P2 Implementation plan 列回填（[Pending]→Done）（v1.12→v1.13） | [human] · Claude Opus 5 (1M context) |
| v1.14 | 2026-09-22 | **P3 brainstorm 裁决回填（grilling 合流，P3 scope 三处修正 + 两新要求）**：① 接线重构 → 零接线——四表判据面唯一实现 = engine lifecycle，本仓运行期每步 dispatch 自身被审（canary 回归运行期形态），validate 无四表 block（12→11 块）、steps 全语义名零编号 anchor（5b0/5b1/12. 族退役）② `canary-dogfood.test.ts` 删除——unit/e2e 测试 = 功能性验证，零本仓产物 fixture（产物删即测试废，耦合病）③ 42 用例全删零迁入（判据面 engine 自造链已覆盖；逐案对照表落盘 P3 design）④ canonical phase-id 语法严格 A（`^P\d+(\.\d+)*$` 点分分层，弃字母后缀；schema 全 8 处 pattern 单点派生 + description 写清）⑤ S3 残留簇 + F8a 随守卫删除自消 + consumer-sim 升级（Q6 裁）⑥ 运维规范落档（零产物 fixture · 零编号 anchor · 零 legacy 豁免 · phase-id 语法 A——docs/maintainers）（v1.13→v1.14） | [human] · Claude Opus 5 (1M context) |
| v1.15 | 2026-09-22 | **P3 spec-review r1 回填（全部 findings 落地 + 用户补充需求）**：P3 scope/acceptance 增补① **skills 调用 cdd 输出零过滤**（禁 `tail`/`head`/`2>&1 \|`/`EXIT=$?`——cdd 已自身优化输出长度，2026-09-22 用户裁决）② consumer-sim 包貌前提实证——先 `build` 后 `pack`（裸 pack 的 dist/cli.mjs 是 jiti 桩、smoke-plan 不在 tarball）③ phase-id pattern 枚举扩 8 处（cells.dependency/hardEdge/softEdge 并入迁移、依赖边用例）④ grep 探针修正（AC1 `-E`、AC2 排除布局字符串、F8a 守卫自身面）+ S3 SKILL:54 摘引实时化（P2 已改 engine 位，无 block 12 字样）+ 对照表落盘本 design（v1.14→v1.15） | [human] · Claude Opus 5 (1M context) |
| v1.16 | 2026-09-22 | **P3 用户裁决回填（defect①/② + 命令出口统一）**：P3 scope/acceptance 增补① **docs-family 命令出口统一**——docs review 完成后 stdout 结果面（status/blocker/handoff，与 task 通道对齐）；`cli/*.ts` 命令级出口禁止裸 `return;`，统一走 `exit.ts` 出口族 + exit.ts 统一抽象（成功+结果面一次调用）；exit table 0/1/2/3 不变 ② **fix 边界条款**——review 后编排者只读 status/blocker 计数、findings 全文由 `cdd fix` fix-agent 消费、不得 inline 应用 findings（defect②：编排者读了 findings 自己改 = 湿滑斜坡，本 P3 spec-review r1 实证）③ 运维规范落档补两条（结果可见性 · exit.ts 出口单源）（v1.15→v1.16） | [human] · Claude Opus 5 (1M context) |
| v1.17 | 2026-09-22 | **P3 design spec-review-3 回填（spec-review-4 修正探针）**：P3 acceptance 零编号 anchor 探针改前缀锚定/整串判定（`^[0-9][0-9.\-bc]*[. ]+[A-Za-z(]` 数字前缀步名全捕获、语义名零误报——node 断言「现名全命中 / 语义名全 miss」防白绿；旧式 `[0-9]+[. ]`+尾 `\b` 失效：尾 `\b` 在「.」→「 」non-word→non-word 边界断、吞点后备选项失配，`12. x`/`7. x`/`6. x`/`0. x` 全部漏检，spec-review-4 实证后改正向后缀 `[A-Za-z(]`，design AC4 同步）；consumer-sim pack 机制实证澄清（`prepare: dev:stub` 钩子移除为主修 + `pnpm pack --config.ignore-scripts=true` 备防；`--ignore-scripts` 旗标 / `npm_config_ignore_scripts` env / root 相对路径 pack 均实证失效；发布路径 `pnpm publish` 同险登记）——细节落 P3 design §2.6/AC6（v1.16→v1.17） | [human] · Claude Opus 5 (1M context) |
| v1.18 | 2026-09-22 | **P3 closeout 回填（backfill-overall · branch-review 前置义务——T8 收口位）**：P3 Design-spec 列回填（[Pending]→p3-design v1.2）；P3 Implementation plan 列回填（[Pending]→Done）；S3 残留簇改写落笔——cross-cutting「本仓四表仍受 scripts 校验直至 P3」→「本仓文档合规 = engine lifecycle 审计（dispatch 运行时 + engine 套件），无 repo 侧守卫」、Maintenance「本程序自身四表仍受…」→ 历史时态 + engine 位，Issue inventory P3 行 / Phase inventory P3 scope cell 逐验无现时时态主张 → 不动；P3 全貌核算 = 守卫退役（overall-consistency / plan-spec-anchors 删除 + 42 用例零迁入）+ validate 接线 11 块 + 零编号 anchor（steps 语义名）+ canonical phase-id 语法 A（8 pattern 单点派生）+ consumer-sim 升级挂 release 门 + docs-family 出口统一（结果可见性 + exit.ts 出口族）+ mid-flight backfill 落点（§2.10，I7 × 五件 SKILL.md）+ 运维规范八项落档 docs/maintainers（program-experience §G）；P3 acceptance 探针形态归一——v1.17 的 `\|` 转义形态（照字面复制为验证 grep 即白绿）换无 pipe 形态 `^[0-9][0-9.\-bc]*[. ]+[A-Za-z(]`（digit-led 首类 + `[bc]` 覆盖 5b/5c 族；现名全命中 / 语义名全 miss 语义保持），change-history v1.17 条目探针同步归一，验证 grep 显式带 `-E`（`grep -E "^[0-9][0-9.\-bc]*[. ]+[A-Za-z(]"`）可直接拷贝或取 ci-validate.test.mjs node 断言面——还原步骤登记于本行（v1.17→v1.18） | [human] · Claude Opus 5 (1M context) |
| v1.19 | 2026-09-23 | **P4 brainstorm 裁决回填（P4 拆点分 P4.1/P4.2 + 文档治理入列）**：P4 拆 `P4.1` 文档治理 / `P4.2` 发布闭环（用户裁决，phase-id 语法 A 点分）；Non-goal #4 修订——README/CLAUDE.md 全面重写由 P4.1 承担（弃现有章节分布与内容的整文档重写，harness 宣称类与行为描述类零分歧，废除旧「不改宣称类」限制）；P4.1 = charter v1.19 + docs/maintainers 重组（文件编号 `NN-name.md` + section 编号，英文主源） + README.md/CLAUDE.md/README.zh-CN.md 重写（zh-CN = 同步 mirror，repo 对外宣讲面）；P4.2 = 版本化整合（cdd-engine 声明值 `1.0.0 → 0.1.0` 基准降值 + p2-major 声称改写 + backlog-6 清除 + version-sync 补 cdd-engine） + 一次集成发版（cdd-engine → 1.0.0 首次稳定开版 breaking 收进 changelog · osuperpowers → 0.2.0） + consumer-sim release 门实测 + pack 审计（osuperpowers 补 files 白名单——现整树进包、tests/scripts 泄漏）；依赖 `P3 -> P4.1 -> P4.2` | [human] · Claude Opus 5 (1M context) (osuperpowers:brainstorming) |
| v1.20 | 2026-09-23 | **cdd spec-review-4 全 finding 落地（3 warn + 2 nit）**：Non-goal #4 重写集补 README.zh-CN.md（同步 mirror 语义，charter 执法权威层与 phase 层三文档口径收敛）；P4.1 scope ② maintainers 项弃内容措辞改正——去「与内容」、显式内容保真迁移（与 acceptance ②「链接迁移完整」、v1.19 行「重组（无弃内容语）」对齐）；P4.1 scope ① + Issue inventory 行 v1.19 时态厘清——改基线 + 前进式表述（正向约束 v1.19 基线已改正，P4.1 落笔期再 v-bump + 四表回填），Issue inventory 行同步同口径；P4.1 acceptance ① 四表同步时态同根厘清（基线 v1.19；backfill-as-version 于 P4.1 程内完成）；P4.1 acceptance 语言政策改述为政策修订语义（mirror 仅 README.zh-CN 属随 CLAUDE.md 重写落笔的政策修订，而非「不破」）（v1.19→v1.20） | [human] · Claude Opus 5 (1M context) |
| v1.21 | 2026-09-23 | **P4.3 注册（用户裁决 · P4.1 执行期 mid-flight backfill 即时落地，Task 2 循环恢复前）**：consumer-parity 新 phase P4.3——① cdd CLI `--task`→`--tasks` 多 task 模式（`--tasks 1` · `--tasks 1,2`）② cli-driven-development 正式 enter loop 前新增 **task groups 裁定**节点（task groups 界定 → 用户确认 → 才进 loop）③ **Mid-Flight Backfill I4 语义歧义修复**（语义 = 任何 dispatch——implement/review/fix——返回即落地 backfill · 暂停循环闭合 · 独立 commit · 干净树后恢复循环）；Issue inventory / Phase inventory 增 P4.3 行；依赖图改 `P3 -> P4.1 -> P4.3 -> P4.2`（P4.2 前驱由 P4.1 改 P4.3——1.0.0 稳定开版消费 --tasks 产物）（v1.20→v1.21） | [human] · Claude Opus 5 (1M context) |
| v1.22 | 2026-09-23 | **P4.1 closeout 回填（backfill-overall · branch-review 前置义务——Task 8 收口位，P4.1 shipped）**：P4.1 Design-spec 列回填（[Pending]→p4.1-design v1.3）；P4.1 Implementation plan 列回填（[Pending]→Done）；Issue inventory P4.1 行逐验无现时时态主张 → 不动（v1.21→v1.22） | [human] · Claude Opus 5 (1M context) |
| v1.23 | 2026-09-23 | **P4.1 branch-review 修正回填（P4.1 行 mirror 口径改正）**：P4.1 Phase-inventory 行 scope ③ 去「其余零 mirror」、acceptance 语言政策去「mirror 仅 README.zh-CN」——改述为 shipped mirror 集 = 根 + 两包三件 `README.zh-CN.md`（2026-09-23 用户裁决 mirror 扩展，p4.1-design §1/§3）；v1.22 closeout 行其余回填不变（v1.22→v1.23） | [human] · Claude Opus 5 (1M context) |
| v1.24 | 2026-09-23 | **P4.1 收尾修正回填（PR #275 评审回馈三条 · 开 PR 待合期 mid-flight backfill 即时落地）**：① docs/maintainers **重新整理**——重新整理重新编号、**包括精简与删除**（用户裁决，纠正 v1.20「内容保真迁移」口径；P4.1 行 scope ② 同步改述）；② `07-osuperpowers-plugin.md` **删除**（唯一值面 release 流程 = `.changeset/README.md` §Release flow；live 引用清扫——CLAUDE.md ×2 · `packages/osuperpowers/README.md`「Docs for maintainers」· maintainers README 索引；frozen specs/plans 历史引用豁免）；③ **英文 README 零中文**——根 + 两包 `README.md` 语言切换行 `[中文]`→`[Simplified Chinese]`，`.zh-CN.md` mirror 不动（P4.1 行 acceptance 同步补述）（v1.23→v1.24） | [human] · Claude Opus 5 (1M context) |
| v1.25 | 2026-09-24 | **P4.1 语言切换行修正回填（用户裁决 2026-09-24：language switch 不用改）**：v1.24 的「英文 README 零 CJK——切换行 `[中文]`→`[Simplified Chinese]`」主张**撤销**——`[中文]` 为语言切换标签的合法原生形态，三件 English `README.md` 切换行维持 `[English](README.md) | [中文](README.zh-CN.md)` 原样、Task 10 的 `[Simplified Chinese]` 改动回滚（`806d8058`）；P4.1 行 acceptance 同口径改述（v1.24→v1.25） | [human] · Claude Opus 5 (1M context) |
| v1.26 | 2026-09-24 | **P4.4 注册 + P4.3 行 grilling 裁决同步（用户裁决 · P4.3 brainstorm 期即时落地）**：consumer-parity 新 phase P4.4——`scripts/` 与 cdd-engine **全面 OOP 化**（P4.3 TaskGroup 单数据模型扩展为 engine 全层类抽象 + scripts 编排面同构 OOP 化；空壳/死代码即删、无过渡 shim、无别名；破坏性变更于 P4.2 1.0.0 开版前收口；用户裁决「先注册不探索」，具体 scope/AC 于其 brainstorm 细析）；Issue inventory / Phase inventory 增 P4.4 行；依赖图改 `P4.1 -> P4.3 -> P4.4 -> P4.2`（P4.2 前驱由 P4.3 改 P4.4——发布面消费 OOP 化完成态）；P4.4 grilling 释放 gate = 前驱 P4.3 Design spec 非 [Pending]（serial-phase：satisfied 前不得释放，v1.21 注册同类裁决制式）；P4.3 行 scope/AC 同步 grilling 裁决——① 单数据模型 gloss（`--tasks` 列表即唯一 dispatch 面、无单/多双面、review 审整组/fix 整组重申）· ② 分组规划落盘 phase plan「Task Groups」节（默认全单组 · 调整独立 commit）· ③ I4 文本承载面 = 五件（AC「三件」改五面承载面枚举：4× writing-* + cli-driven-development）（v1.25→v1.26） | [human] · Claude Opus 5 (1M context) |
| v1.27 | 2026-09-24 | **P4.3 scope 扩展（用户裁决 · P4.3 design 批准后并入 #274/#276）**：P4.3 增两项 cdd-engine doc-contract gate 修复——④ `[In-flight]` 中间态 + claim 等值放宽 + 子句 prose 误命中诊断（#274：closeout 规则新合法态 `[Pending]` → `[In-flight]` → `**Done**`，in-flight 无 reverse claim 义务，为四表纪律的明示 carve-out）⑤ overall.json 描述↔enforcement 三处对齐 + 报错附 `should look like:` 正确形态（#276）；Issue inventory 增两锚点行（#274/#276，锚点注册域）；P4.3 行 scope/AC 增 ④⑤；依赖不变 `P4.1 ->(hard)`；两修同属 engine breaking 面、1.0.0 前窗口；写 spec 前同步（overall wins on conflict，v1.26→v1.27） | [human] · Claude Opus 5 (1M context) |
| v1.28 | 2026-09-24 | **P4.3 scope 扩展（用户裁决 · P4.3 design 批准期并入 cdd schema get）**：P4.3 增 ⑥——`cdd schema get <overall, phase-spec, plan>` 发现型子命令直出 canonical schema（stdout · 零执法逻辑）+ skills（writing-* read-schema 节点）从「`cdd help` → 目录 → Read 文件」改「`cdd schema get` 直取成文」（砍跨系统文件查找心智负担）；**Non-goal #1 修订**——唯一例外 `cdd help` → `cdd help` + `cdd schema get` 双发现型豁免；Cross-cutting「skills 经 `cdd help` 发现通道直取 schema」改述 schema-get 通道（`cdd help` 仍打印目录面）；Issue inventory 增 P4.3 none 锚点行；P4.3 行 scope/AC 增 ⑥；依赖不变 `P4.1 ->(hard)`；engine breaking 面 1.0.0 前窗口（v1.27→v1.28） | [human] · Claude Opus 5 (1M context) |
| v1.29 | 2026-09-24 | **P4.3 行形状矫正（doc-contract pre-flight 拦截实证 · v1.28 数据修正）**：v1.28 的 P4.3 phase 行 scope ⑥/ac 追加与 Non-goal/changelog 中 `cdd schema get <overall, phase-spec, plan>` 的初写形态以竖线符分隔类型名，被 Phase-inventory 行解析为 cell 分隔符 → 行 12 cells ≠ norm 8，连带 Issue 行 phase 认不出 + 依赖图 dangling 级联；全部改逗号分隔形态（overall ×4 处 + spec §2.7/AC 两处），行形态复原 8 cells；本轮记录 = docContractValidate pre-flight 即拦截行形状漂移的实证（v1.28→v1.29） | [human] · Claude Opus 5 (1M context) |
| v1.30 | 2026-09-24 | **P4.3 ⑥ 细化回填（用户裁决 · help 简化 + 类型枚举校准）**：`cdd schema get` doc-type 枚举校准为 engine `DOC_SCHEMA_NAMES` 全量四件（overall · plan · phase-spec · add-phase-protocol——修 spec-review-3 对齐的注册表层，Non-goal #1 措辞随改）；**`cdd help` 简化**——`schemas:` 行删除（其唯消费者 read-schema 节点已全改 `cdd schema get`，doc-structure 内容面唯经 schema get 直取；help 保留 cli/templates 两行，templates 面含 handoff schema 等资源定位）；P4.3 行 scope/AC ⑥ 同步（AC writing-* 面修正为三件 + single-spec N/A + help 两行断言）；Cross-cutting「`cdd help` 仍打印 CLI/schema 目录面」改述 v1.30 形态；依赖不变 `P4.1 ->(hard)`（v1.29→v1.30） | [human] · Claude Opus 5 (1M context) |
| v1.31 | 2026-09-24 | **P4.2/P4.4 scope 扩展（用户裁决 · P4.3 执行期 mid-flight backfill 即时落地，T2 收口后回落）**：P4.2 增「理念宣讲篇幅」（README 增 cdd + osuperpowers 理念介绍，尤其 cdd 理念——cdd 是什么 · 设计动机 · 三种模式链 · Review Convergence 等收敛纪律；P4.1 行为一致性重写基础上的宣讲增量）与「docs/maintainers 运维文档精炼」（现有体量太大太杂，收尾收敛：删冗余 / 合碎篇 / 精简篇幅）；P4.4 增「biomejs 全面接入」（husky pre-commit 钩子 + biome lint/format 全仓 ts 面落地，静态质量门并列既有 precommit 校验链）；Issue inventory P4.2 行 / P4.4 行 scope `none` 锚点行同步增补；Phase inventory P4.2 / P4.4 scope/AC 增补；依赖不变 `P4.4 ->(hard)` / `P4.3 ->(hard)`（v1.30→v1.31） | [human] · Claude Opus 5 (1M context) |
| v1.32 | 2026-09-24 | **P4.3 ⑥ `cdd help` 子命令整体移除（用户裁决 · P4.3 执行期 mid-flight backfill 即时落地，T6 收口后回落）**：`cdd schema get` + skills read-schema 直取（Task 5/6）后，`cdd help` 发现子命令的三行目录面（cli/schemas/templates）全空消费者——schemas 面唯消费者 read-schema 已改 `cdd schema get`（v1.28/1.30）、cli/templates 全仓零 agent 消费；v1.30「保留 cli/templates 两行」裁决废止，**整体移除 `cdd help` 子命令**（`--help` 旗标 = citty usage 渲染面，非子命令、保留）；Phase inventory P4.3 行 scope/AC ⑥ 同步（AC：`cdd help` 四文件删除 + grep 零命中 + smoke-cdd 改锚 `cdd schema get plan`）；Non-goal #1 双发现型豁免缩为单发现型 `cdd schema get`；Cross-cutting「`cdd help` 打印 CLI/templates 目录面」改述整体移除形态；依赖不变 `P4.1 ->(hard)`（v1.31→v1.32） | [human] · Claude Opus 5 (1M context) |
| v1.33 | 2026-09-25 | **repo 定位改述 cdd 方法论（2026-09-24 用户裁决 · P4.3 执行期 mid-flight backfill 仅记录，暂不落地——待 P4.2 阶段启动时分析）**：repo 不再是「个人 AI 编程技能市场」，改述「基于 cdd 理念的方法论」（README 家族 + CLAUDE.md 定位句 + 宣讲面措辞——v1.31 理念宣讲篇幅的增量承接）；并用 gh 更新 GitHub 仓库元信息（Oscaner/skills · PUBLIC · 现 desc=`About Skills for Real Engineers. Straight from my experience.` · 无 topics · 无 homepageUrl）；**本裁决仅记录不实现**——P4.2 阶段启动（grilling 枚举）时纳入分析落地；Phase inventory P4.2 行 scope 增补 + Issue inventory P4.2 行同步；依赖不变 `P4.4 ->(hard)`（v1.32→v1.33） | [human] · Claude Opus 5 (1M context) |
| v1.34 | 2026-09-25 | **P4.3 closeout 回填（backfill-overall · branch-review 前置义务——P4.3 plan complete 10/10，P4.3 shipped）**：P4.3 Design-spec 列回填（[Pending]→p4.3-design v1.7）；P4.3 Implementation plan 列回填（[Pending]→Done）；engine 终态并入声明源（v1.33→v1.34） | [human] · Claude Opus 5 (1M context) |
| v1.35 | 2026-09-25 | **P4.3 reopen + Task 11（用户裁决 · branch-review 后 mid-flight：skill 骨架结构从文本指导迁移为 canonical schema 单源）**：P4.3 Implementation plan 列 `Done` → `[In-flight]`（reopen 展开 Task 11——skill-anatomy 骨架 schema + validate 严格 allowlist 机器校验 + 06-skill-authoring.md/CLAUDE.md 文本指导清理；strict 约束：注册表外章节/标题 → BLOCK，skill 是消费者真实入口面零不确定性）；closeout claim 待二次终结态回填（reopen → re-close）；P4.3 行 scope/AC 增 ⑦；依赖不变 `P4.1 ->(hard)`（v1.34→v1.35） | [human] · Claude Opus 5 (1M context) |
| v1.36 | 2026-09-25 | **P4.3 re-close 终结回填（backfill-overall · 二次终结——plan reopen 后 Task 11 完成 11/11，P4.3 shipped）**：P4.3 Implementation plan 列 `[In-flight]` → `Done`（Task 11 skill-anatomy 骨架 schema 落地：`DOC_SCHEMA_NAMES` 五件 + validate 严格 allowlist 机器校验 + 06-skill-authoring.md `git rm` + CLAUDE.md 引用改向，plan complete 终态并入声明源）；change-history 二次 claim 落地；P4.3 行 scope/AC ⑦ 保持；依赖不变 `P4.1 ->(hard)`（v1.35→v1.36） | [human] · Claude Opus 5 (1M context) |
| v1.37 | 2026-09-25 | **P4.3 行 scope ⑦ 措辞修订（branch-review nit fix · live charter 面去退役文件名）**：P4.3 行 ⑦「替代 06-skill-authoring 文本指导」改「替代 skill-authoring 文本指导」——live charter 面不再承载已 `git rm` 的旧编号文档名（与 Task 11 ③「06-skill-authoring 字样零残留 · frozen 豁免」的主旨一致；v1.35/v1.36 冻结 change-history 行按原样保留）；P4.3 行 scope/AC 其余不变；依赖不变 `P4.1 ->(hard)`（v1.36→v1.37） | [human] · Claude Opus 5 (1M context) |
