# gate/

Node CDD gate surface (P4b). 核心 + adapters + configs + tests.

- `cdd-gate-core.mjs` — Node gate core (placeholder in T1, lands in T2).
- `adapters/` — harness adapters (T3+, replacing `bin/override-*-cdd-gate.sh`).
- `configs/` — gate configuration (T4+).
- `tests/` — gate tests (T2+).
