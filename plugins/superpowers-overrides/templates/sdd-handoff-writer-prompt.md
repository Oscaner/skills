# Handoff writer dispatch

**Phase:** {{phase}}

**Handoff path:** {{handoff_path}}

**Read only these paths (do not Read anything else):**

{{paths_list}}

**Write:** Update `{{handoff_path}}` per spor-handoff-writer SKILL.md.

**On CHANGES_REQUESTED (review segment):** also write open-findings JSON from handoff findings.

**Return:** H1 four-line contract only (`status:` / `commits:` / `artifacts:` / `blocker:`).

**Model:** cheapest capable tier — structured extraction only.
